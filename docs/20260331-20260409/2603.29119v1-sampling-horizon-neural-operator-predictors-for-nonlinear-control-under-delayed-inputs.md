---
title: Sampling-Horizon Neural Operator Predictors for Nonlinear Control under Delayed Inputs
title_zh: 用于输入延迟下非线性控制的采样时域神经算子预测器
authors: "Luke Bhan, Peter Quawas, Miroslav Krstic, Yuanyuan Shi"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29119v1"
tags: ["query:pde"]
score: 9.0
evidence: 用于非线性控制系统的神经算子预测反馈
tldr: 本文针对具有输入延迟和采样测量的非线性系统控制问题，提出两种神经算子预测反馈设计，分别在均匀和非均匀采样下实现延迟补偿。方法保证系统半全局实用稳定，并在仿真中实现精确跟踪与25倍速度提升，揭示采样灵活性与近似精度间的折中。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.29119v1/fig-001.webp\", \"caption\": \"Fig. 1: Approximate predictor-feedback implementations under uniform and bounded sampling.\", \"page\": 2, \"index\": 1, \"width\": 457, \"height\": 531}, {\"url\": \"assets/figures/arxiv/2603.29119v1/fig-002.webp\", \"caption\": \"Fig. 2: (a) Joint 5 tracking with delay D = 0.2s. (b) Measurement sampling schedules: numerical and Case 2 use randomized intervals in [0.02, 0.1], while Case 1 uses fixed sampling h = 0.05. Measurements include Gaussian noise N (0, 0.01). (c) Moving average (0.25s) of the L2 tracking error. (d) Prediction error - zoomed to the first 1s.\", \"page\": 5, \"index\": 2, \"width\": 935, \"height\": 490}]"
motivation: 为解决输入延迟和采样状态测量下预测反馈控制计算成本高和依赖连续测量的问题。
method: 提出两种基于神经算子的预测反馈设计，一种使用采样区间预测算子，另一种仅近似延迟补偿预测器并结合闭环流控。
result: 在六连杆非线性机器人系统实验中，两种方法保持精确轨迹跟踪并实现约25倍计算加速。
conclusion: 研究表明，基于神经算子的预测反馈控制能够在保证稳定性的同时显著降低非线性系统延迟控制中的计算开销。
---

## 摘要
现代控制系统常在存在输入延迟和采样状态测量的情况下运行。常见的延迟补偿策略是预测反馈；然而，实际实现通常需要在线求解一个隐式常微分方程，导致计算成本难以承受。此外，预测器设计通常假设状态测量是连续可得的，而在实际中，测量可能是采样的、不规则的，或因硬件故障而暂时缺失。本文针对具有输入延迟和采样测量的非线性系统，提出了两种基于神经算子的预测反馈设计。第一种设计引入了一个采样时域预测算子，将当前测量与输入历史映射到下一个采样区间内的预测状态轨迹。第二种设计中，神经算子仅逼近延迟补偿预测器，然后与测量之间的闭环动态组合使用。第一种方法要求均匀采样，但其残差上界可直接随算子逼近误差而定。相比之下，第二种方法可适应非均匀但有界的采样计划，代价是逼近误差的放大，这揭示了控制工程师在采样灵活性与逼近敏感性之间的实际权衡。对这两种方案，我们都建立了基于神经算子误差显式界限的半全局实用稳定性。数值实验在一个六连杆非线性机械臂上验证了该方法的准确跟踪能力，并相较基线方法实现了 25 倍的显著计算加速。

## Abstract
Modern control systems frequently operate under input delays and sampled state measurements. A common delay-compensation strategy is predictor feedback; however, practical implementations require solving an implicit ODE online, resulting in intractable computational cost. Moreover, predictor formulations typically assume continuously available state measurements, whereas in practice measurements may be sampled, irregular, or temporarily missing due to hardware faults. In this work, we develop two neural-operator predictor-feedback designs for nonlinear systems with delayed inputs and sampled measurements. In the first design, we introduce a sampling-horizon prediction operator that maps the current measurement and input history to the predicted state trajectory over the next sampling interval. In the second design, the neural operator approximates only the delay-compensating predictor, which is then composed with the closed-loop flow between measurements. The first approach requires uniform sampling but yields residual bounds that scale directly with the operator approximation error. In contrast, the second accommodates non-uniform, but bounded sampling schedules at the cost of amplified approximation error, revealing a practical tradeoff between sampling flexibility and approximation sensitivity for the control engineer. For both schemes, we establish semi-global practical stability with explicit neural operator error-dependent bounds. Numerical experiments on a 6-link nonlinear robotic manipulator demonstrate accurate tracking and substantial computational speedup of 25$\times$ over a baseline approach.

---

## 论文详细总结（自动生成）

# 用于输入延迟下非线性控制的采样时域神经算子预测器 — 中文总结

---

## 1. 核心问题与研究动机

- **研究背景**：  
  现代控制系统（如机器人或远程控制系统）普遍存在**输入延迟**与**采样测量问题**。输入延迟源于执行器响应时间或通信延迟，而采样测量常受限于传感器刷新率与通信带宽。  
- **传统问题**：  
  延迟补偿常依赖**预测反馈控制器（predictor feedback）**，其核心是预测系统的未来状态来抵消输入延迟。但这种预测需要**在线求解隐式微分方程（ODE）**，计算开销大，且假设**连续测量**并不符合现实。  
- **论文目标**：  
  作者希望通过**神经算子（Neural Operator）方法**，将延迟补偿的预测器部分以离线学习的方式替代，从而：
  - 降低运行时计算复杂度；
  - 适应仅有离散测量甚至非均匀采样的环境；
  - 保留控制系统理论的稳定性保证。

---

## 2. 方法论概述

### 2.1 核心思想
论文提出两种神经算子预测反馈控制方法：

1. **Case 1：采样时域预测算子（Sampling-Horizon Prediction Operator）**
   - 在离线阶段，训练一个神经算子 \( \hat{M} \)，直接从当前测量状态与输入历史预测未来一个采样周期 \( h \) 内的状态轨迹。
   - 实时控制时，控制律直接基于预测轨迹实现延迟补偿。
   - **优点**：无需积分，计算快速；残差上界与算子逼近误差 \(\epsilon\) 成正比。
   - **限制**：要求**采样周期固定（均匀采样）**。

2. **Case 2：预测器算子（Predictor Operator）**
   - 神经算子仅近似延迟补偿预测器 \( P(X,U) \)，用于生成预测的初始条件。
   - 再通过标准闭环动态方程积分获得未来状态。
   - **优点**：可支持**非均匀但有上界**的采样。
   - **代价**：残差误差会放大为 \(\epsilon e^{C_f(1 + C_\kappa)h}\)，要求更高精度的神经算子。

### 2.2 理论推导与稳定性分析

- **主要假设**：
  - 系统 \( \dot{X}(t) = f(X(t),U(t-D)) \) 满足 Lipschitz 连续；
  - 延迟自由闭环系统可稳定；
  - 系统具有输入到状态稳定性（ISS）。
- **主要结论**：
  - 提出**采样时域算子 \( M = Z(P(X,U)(0)) \)** 的定义；
  - 分别证明 \( P \) 和 \( Z \) 的 Lipschitz 连续性；
  - 通过类 \( \mathcal{K} \)、类 \( \mathcal{KL} \) 函数建立半全局实用稳定性定理；
  - 稳定性残差与神经算子逼近误差显式相关。

### 2.3 算法流程（文字化）
1. **离线阶段**：收集输入历史与状态轨迹数据；使用 Fourier Neural Operator（FNO）训练神经算子近似 \( P \) 或 \( M \)。  
2. **在线控制阶段**：
   - 在采样时刻 \( T_i \)，读取状态 \( X(T_i) \)；
   - 使用神经算子生成预测轨迹 \( \hat{Z}(t) \)；
   - 执行控制律 \( U(t) = \kappa(\hat{Z}(t)) \)；
   - 重复更新。

---

## 3. 实验设计

- **实验系统**：  
  六连杆非线性机械臂（xArm6），系统状态维度为 12（含角度与角速度），控制信号为关节转矩。设输入延迟 \( D = 0.2\,s \)。

- **控制目标**：  
  轨迹跟踪任务 \( X_{des}(t) = X_0 + A \sin(\omega t + \phi) \)。

- **测试设置**：
  - 加入高斯测量噪声 \( N(0,0.01) \)；
  - Case 1：固定采样周期 \( h = 0.05\,s \)；
  - Case 2：随机采样间隔 ∈ [0.02, 0.1]。

- **对比基线**：
  1. **Numerical Predictor Feedback**（解析实现的经典方法）；
  2. **Case 1**：神经算子采样时域预测；
  3. **Case 2**：神经算子预测器 + 动态积分。

---

## 4. 资源与算力

- 文中指出：
  - 使用 **NVIDIA RTX 4070 GPU** 加速；
  - 神经算子单次计算**约 1 ms**；
  - 传统数值预测器计算 **约 25 ms**；
  - 因此获得 **约 25×** 的运行速度提升。  
- 未给出具体训练轮次、批量大小或时长等细节，但说明离线阶段利用**20,000 条含噪轨迹样本**进行训练，网络验证误差 \( L_2 = 10^{-4} \)。

---

## 5. 实验数量与充分性

- **实验数**：主要展示三类实验方案（数值预测、Case 1、Case 2）在相同任务下的比较；未报告多系统泛化或消融实验。  
- **充分性**：
  - 实验覆盖了**均匀与非均匀采样、噪声干扰、长延迟**三类关键实际因素；
  - 然而系统仅限于**一个高维机械臂模型**，缺乏跨领域验证；
  - 对神经算子复杂度、训练数据规模、延迟长度变化的消融评估仍缺少。

---

## 6. 主要结论与发现

- 两种神经算子预测反馈方法均能保证**半全局实用稳定性**；
- 残差上界与神经算子逼近误差呈显式函数关系；
- 当采样均匀时，系统鲁棒性更高、实现更简单；
- 当采样非均匀时，需提高近似精度以维持同等级稳定性；
- 实验验证了方法对复杂非线性系统的可行性与显著计算加速能力。

---

## 7. 优点与创新

- **理论与学习融合**：在传统非线性控制理论中引入神经算子近似，保持稳定性分析的可证明性；
- **灵活的采样建模**：兼顾固定与非固定采样两种实际场景；
- **算子级学习**：利用 Fourier Neural Operator 近似函数空间映射，而非仅点对点预测；
- **显著计算效率**：实现 25 倍以上推理加速；
- **提供显式误差边界**：可指导控制器稳定性与神经网络精度的权衡。

---

## 8. 不足与局限

- **实验覆盖有限**：仅验证单一机器人系统，缺少跨系统泛化实验；
- **精度依赖数据分布**：神经算子需在输入历史的紧致域上训练，离线训练分布若与在线运行不匹配，稳定性保证可能失效；
- **非均匀采样误差放大**：对延迟大、采样稀疏的系统敏感；
- **训练细节不透明**：论文未详细说明训练耗时、硬件数量、超参数选择；
- **理论假设较强**：如要求 \(f\)、\(\kappa\) 满足 Lipschitz 连续性，且系统强正向完备，对一些实际系统不易验证。

---

**（完）**
