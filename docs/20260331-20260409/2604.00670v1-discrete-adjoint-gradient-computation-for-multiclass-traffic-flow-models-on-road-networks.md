---
title: Discrete adjoint gradient computation for multiclass traffic flow models on road networks
title_zh: 道路网络上多类别交通流模型的离散伴随梯度计算
authors: "Paola Goatin, Axel Klar, Carmen Mezquita-Nieto"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00670v1"
tags: ["query:pde"]
score: 10.0
evidence: 使用双曲守恒律系统和边界条件的道路网络交通流模型
tldr: 本文提出了一种用于多类别交通流网络优化的离散伴随梯度计算方法，通过Godunov型有限体积离散和复杂交叉口耦合条件实现高效敏感度分析，经实验证明该方法能在大规模网络中高效优化旅行时间与距离。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.00670v1/fig-001.webp\", \"caption\": \"Fig. 5 Partial derivative terms in ∂yE are ordered first by time and then by cell index. Blocks where time iteration k /∈ {ν − 1, ν} are 0.\", \"page\": 11, \"index\": 1, \"width\": 755, \"height\": 368}, {\"url\": \"assets/figures/arxiv/2604.00670v1/fig-002.webp\", \"caption\": \"Fig. 8 Plot of the level curves for the TTD for two different control variables, where the fixed parameters have the values defined in (26). In (a) and (b), the class is fixed, while in (c) and (d), the junction is fixed.\", \"page\": 15, \"index\": 2, \"width\": 667, \"height\": 606}, {\"url\": \"assets/figures/arxiv/2604.00670v1/fig-003.webp\", \"caption\": \"Fig. 7 Plot of the level curves for the TTT for two different control variables, where the fixed parameters have the values defined in (26). In (a) and (b), the class is fixed, while in (c) and (d), the junction is fixed.\", \"page\": 14, \"index\": 3, \"width\": 669, \"height\": 607}, {\"url\": \"assets/figures/arxiv/2604.00670v1/fig-004.webp\", \"caption\": \"Fig. 9 Results comparing the discrete adjoint gradient and the finite difference computation for a Matlab R2024a fmincon optimization with an sqp algorithm, with increasing values of a = log2 n, a = 0, . . . , 6, where n is the number of time-interval subdivisions.\", \"page\": 18, \"index\": 4, \"width\": 721, \"height\": 641}, {\"url\": \"assets/figures/arxiv/2604.00670v1/fig-005.webp\", \"caption\": \"Fig. 10 Plot of the Pareto front of the TTT against the TTD for different time-interval subdivisions n.\", \"page\": 19, \"index\": 5, \"width\": 395, \"height\": 352}, {\"url\": \"assets/figures/arxiv/2604.00670v1/fig-006.webp\", \"caption\": \"Fig. 6 Diagram of the test network showing the different roads and their associated parameters. Junctions e = 2, 3 diverge and junctions e = 4, 5 merge.\", \"page\": 12, \"index\": 6, \"width\": 633, \"height\": 272}, {\"url\": \"assets/figures/arxiv/2604.00670v1/fig-007.webp\", \"caption\": \"Fig. 11 Results comparing the total vehicle densities on roads 1, 2 and 6 of the network represented in Figure 6 for some of the points shown in Figure 10, together with the initial point (1439, 29865). Point (b) minimizes TTT over TTD, point (c) minimizes both functionals and point (d) minimizes TTD over TTT.\", \"page\": 20, \"index\": 7, \"width\": 743, \"height\": 635}]"
motivation: 为提高道路网络中多类别交通流的优化效率，减少总旅行时间与距离。
method: 采用离散伴随梯度计算方法优化多类别交通流模型，通过Godunov型有限体积离散和复杂交叉口耦合机制实现高效敏感度分析。
result: 数值模拟显示该方法在控制参数增多时仍具有高效性和稳定性。
conclusion: 该方法在大规模交通网络优化中具有良好的计算效率和适用性。
---

## 摘要
本文将离散伴随梯度计算方法应用于道路网络上的多类别交通流模型。不同类别的车辆通过其特定的速度函数加以区分，这些速度函数依赖于总交通密度，从而形成一个耦合的双曲型守恒律系统。该系统采用基于需求与供给函数的 Godunov 型有限体积格式离散化，并扩展以处理复杂的路口耦合条件——如汇流与分流——以及包含缓冲长度的边界条件，以考虑拥堵回溢的影响。对不同出行相关性能指标（包括总出行时间和总出行距离）的优化被表述为一个约束最小化问题，并通过伴随梯度方法实现，从而能够高效计算相对于所选时变控制变量的灵敏度。在一个示例网络上的数值模拟表明，所提出的框架效率较高，尤其是在控制参数数量增加的情况下。该方法提供了一种稳健且计算高效的解决方案，适用于大规模交通网络优化。

## Abstract
This paper applies a discrete adjoint gradient computation method for a multi-class traffic flow model on road networks. Vehicle classes are characterized by their specific velocity functions, which depend on the total traffic density, resulting in a coupled hyperbolic system of conservation laws. The system is discretized using a Godunov-type finite volume scheme based on demand and supply functions, extended to handle complex junction coupling conditions -- such as merges and diverges -- and boundary conditions with buffer lengths to account for congestion spillback. The optimization of different travel-related performance metrics, including total travel time and total travel distance, is formulated as a constrained minimization problem and is accomplished through the use of an adjoint gradient approach, allowing for an efficient computation of sensitivities with respect to the chosen time-dependent control variables. Numerical simulations on a sample network demonstrate the efficiency of the proposed framework, particularly as the number of control parameters increases. This approach provides a robust and computationally efficient solution, making it suitable for large-scale traffic network optimization.

---

## 论文详细总结（自动生成）

# 道路网络上多类别交通流模型的离散伴随梯度计算 — 论文总结

---

## 一、研究核心问题与整体含义

- **研究背景与动机**  
  交通网络的复杂性在于存在不同类别的车辆（如轿车、货车、公交等），其速度和行为特性不同。随着交通需求和实时控制需求增长，传统单类别模型已无法有效刻画多类车辆之间的交互效应。  
  本论文关注**多类别交通流模型（multiclass traffic flow model）**的最优控制问题，旨在通过高效的梯度计算方法改进交通调度与优化，降低总旅行时间（TTT）与总旅行距离（TTD），提升网络运行效率。

- **核心问题**  
  在多类别交通模型中，控制变量（如分流比例、优先级、限速）数量巨大，梯度计算成为瓶颈。论文的核心问题是：
  > 如何在离散化的多类别交通流 PDE 网络中，**高效计算梯度**以支持大规模优化？

---

## 二、方法论：离散伴随梯度计算方法

- **总体框架**
  - 模型基于 **双曲型守恒律系统**：  
    \[
    \rho_t + F(\rho)_x = 0
    \]
    其中每个车辆类别 \( c \) 的通量定义为 \( F_c(\rho) = v_c(r)\rho_c \)，总密度 \( r = \sum_{c=1}^N \rho_c \)。
  - 每类车辆速度 \( v_c(r) \) 依赖总密度，形成耦合关系。

- **离散化方法**  
  采用 **Godunov 型有限体积格式**，基于需求（Demand）和供给（Supply）函数离散流量：
  \[
  F_{c,j+1/2} = \rho_{c,j} r_j \min(D_c(r_j), S_c(r_{j+1}))
  \]
  保证数值稳定性和物理合理性，满足 CFL 条件。

- **边界与耦合条件**
  - 对不同类型路口（合流 M×1、分流 1×M、一般 n×m junction）定义守恒条件与分配矩阵。  
  - 引入 **缓冲长度**（buffer length）处理拥堵回溢问题，保证车辆计数一致性。

- **优化模型**
  - 约束优化问题形式：
    \[
    \min_{u \in U} J(u, y), \quad s.t. \ E(u, y) = 0
    \]
    其中 \( u \) 为控制变量（限速、分配矩阵、优先向量），\( y \) 为状态变量（车辆密度与缓冲长度）。
  - 成本函数包括：
    - 总旅行时间（TTT）
    - 总旅行距离（TTD）
  - 梯度计算基于伴随方程：
    \[
    (\partial_y E)^T \xi = -(\partial_y J)^T, \quad \frac{dJ}{du} = \partial_u J + \xi^T \partial_u E
    \]
    其中伴随变量 \( \xi \) 由反向替代求解，利用矩阵稀疏性实现高效计算。

- **算法特点**
  - 坚持“**先离散、后优化**”策略：在离散方程体系上直接构造伴随系统，而非从连续形式推导。
  - 可显著节省梯度计算成本，由 O((NMT)^2+(NMT)(NUT)) 降至较低数量级。

---

## 三、实验设计

- **场景与数据**
  - 自建 **示例交通网络**（7条道路、6个路口，含分流与合流结构）。
  - 考虑 **两类车辆（快车、慢车）**，速度函数采用 **Greenshields** 模型：
    \[
    v_{ℓ,c} = V_{ℓ,c}\left(1 - \frac{r}{R_{ℓ,c}}\right)
    \]
  - 控制变量为各路口分流系数 \( \alpha_c, \beta_c \)。

- **实验方法比较**
  1. **离散伴随梯度法（本文提出）**
  2. **中心差分法（finite difference）**

- **优化目标**
  - 分别优化总旅行时间（TTT）与总旅行距离（TTD）；
  - 进行多目标优化生成 **Pareto 前沿**，展示两者权衡关系。

- **使用工具**
  - MATLAB R2024a，使用 `fmincon`（单目标）和 `gamultiobj`（多目标）优化算法。

---

## 四、资源与算力

- 文中未使用深度学习或 GPU 加速，仅在 CPU 上执行。  
  - 机器配置：Intel Core i5-1335U（13代，1.30GHz）笔记本电脑。  
  - 无批量训练或大规模数据，因此未提到 GPU、训练时间或算力消耗。

---

## 五、实验数量与充分性

- **实验数量**
  - 两组主要实验：
    1. **伴随梯度 vs 有限差分比较实验**：考察不同时间区间划分数量（n = 2⁰,…,2⁶）。
    2. **多目标优化实验**：生成 Pareto 曲线（有多组时间分区 n）。
  - 附加分析：不同时段、不同控制点性能变化；流量密度动态曲线展示。

- **实验充分性**
  - 对比了两种优化方法的**精度、迭代次数、函数评估次数与计算时间**；
  - 多目标实验进一步验证方法在不同目标下的灵活性；
  - 实验虽然基于小网络，但充分展现算法性能趋势与优化行为。

---

## 六、主要结论与发现

1. 离散伴随梯度法在控制参数数量较大时显著提高计算效率，  
   比有限差分法更稳定、更具可扩展性。
2. 随着控制点增多，有限差分法计算时间与迭代次数急剧上升，甚至无法收敛；
   而伴随法保持稳定复杂度。
3. 多目标优化（TTT vs TTD）展现出旅行时间与距离之间的非线性权衡关系；
   Pareto 前沿清晰揭示了交通管理的多策略选择。
4. 方法具备良好的实时性，适用于嵌入 GPS 路径调整与智能交通控制系统。

---

## 七、优点与亮点

- **算法层面**
  - 利用离散矩阵的稀疏下三角结构实现高效伴随求解；
  - “先离散后优化”策略更贴合数值模拟，避免连续模型误差传播。
- **适用性**
  - 可直接应用于多类别车辆调度问题；
  - 支持时变控制与复杂路口耦合。
- **系统视角**
  - 明确提出多目标优化的交通指标分析框架；
  - 对未来实时交通决策具有很强实用价值。

---

## 八、不足与局限

- **网络规模限制**
  - 数值测试仅在小型网络（7条路）上进行；
  - 缺乏大规模城市网络验证。
- **模型简化**
  - 假设类间速度关系单调且无随机扰动，忽略了驾驶行为差异。
- **优化算法**
  - 使用通用优化器（Matlab），未证明在更复杂约束或非凸情形下的稳定性。
- **多目标扩展**
  - Pareto 前沿计算在时间区间过多时精度下降，存在过参数化问题。
- **计算资源**
  - 尽管方法高效，但缺乏对并行或高性能实现的讨论。

---

（完）
