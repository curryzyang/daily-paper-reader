---
title: On Switched Event-triggered Full State-constrained Formation Control for Multi-vehicle Systems
title_zh: 多车辆系统的切换事件触发全状态受约束编队控制
authors: "Zihan Li, Ziming Wang, Xin Wang"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.10993v1"
tags: ["query:profile-2"]
score: 8.0
evidence: 智能交通系统中的车辆编队控制
tldr: 本文针对自动驾驶车辆编队控制中的状态约束与控制奇异性问题，提出了基于平滑非线性映射、RBF神经网络和切换事件触发机制的自适应控制框架。该方法既保证状态约束下的稳定编队，又有效减少通信更新次数。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.10993v1/fig-001.webp\", \"caption\": \"Fig. 2: Safety admissibility audit for longitudinal and lateral velocities of the AV formation.\", \"page\": 5, \"index\": 1, \"width\": 1030, \"height\": 423}, {\"url\": \"assets/figures/arxiv/2604.10993v1/fig-002.webp\", \"caption\": \"Fig. 1: Longitudinal and lateral tracking performance of the vehicle formation control and safe distances in control process.\", \"page\": 5, \"index\": 2, \"width\": 1035, \"height\": 312}, {\"url\": \"assets/figures/arxiv/2604.10993v1/fig-003.webp\", \"caption\": \"Fig. 3: The Longitudinal update time interval of SETM law for the AVs in square formation control process.\", \"page\": 6, \"index\": 3, \"width\": 493, \"height\": 378}]"
motivation: 现有车辆编队控制在状态约束边界易出现奇异性和过大控制输入，影响实际应用。
method: 提出平滑非线性映射结合RBF神经网络自适应控制与切换事件触发机制的综合方案。
result: 仿真结果表明系统信号保持有界，无Zeno现象，编队稳定且通信负担降低。
conclusion: 该研究验证了所提出的控制方法能在状态约束下实现稳定编队并有效减少通信。
---

## 摘要
车辆编队控制是智能交通系统（ITS）的重要组成部分。在实际应用中，控制器设计需要满足多种状态约束，包括车辆间距和车速。当系统状态接近约束边界时，可能会出现控制奇异性和过大的控制作用，从而限制现有方法的实用性。为解决这一问题，本文针对具有不确定动力学的自主车辆（AVs）非线性编队系统进行研究，提出了一种切换事件触发控制框架。首先，引入一种平滑的非线性映射，将受约束的状态空间转换为无约束空间，从而避免在约束边界附近产生奇异性。随后，利用径向基函数神经网络（RBFNN）在线逼近未知的非线性动力学，并基于此通过反步法构建自适应控制器。另外，设计了一种切换事件触发机制（SETM），在瞬态阶段提高控制更新频率，在稳态阶段降低通信负载。基于Lyapunov的分析证明闭环系统中的所有信号均保持一致有界，并排除了Zeno行为。仿真结果验证了所提出方法能够在给定状态约束下实现稳定的车队编队，同时显著减少通信更新。

## Abstract
Vehicular formation control is an important component of intelligent transportation systems (ITSs). In practical implementations, the controller design needs to satisfy multiple state constraints, including inter-vehicle spacing and vehicle speed. When system states approach the constraint boundaries, control singularity and excessive control effort may arise, which limits the practical applicability of existing methods. To address this problem, this paper investigates a class of nonlinear vehicular formation systems for autonomous vehicles (AVs) with uncertain dynamics and develops a switched event-triggered control framework. A smooth nonlinear mapping is first introduced to transform the constrained state space into an unconstrained one, thereby avoiding singularity near the constraint boundaries. A radial basis function neural network (RBFNN) is then employed to approximate the unknown nonlinear dynamics online, based on which an adaptive controller is constructed via the backstepping technique. In addition, a switched event-triggered mechanism (SETM) is designed to increase the control update frequency during the transient stage and reduce the communication burden during the steady-state stage. Lyapunov-based analysis proves that all signals in the closed-loop system remain uniformly bounded and that Zeno behavior is excluded. Simulation results verify that the proposed method achieves stable platoon formation under prescribed state constraints while significantly reducing communication updates.

---

## 论文详细总结（自动生成）

# 多车辆系统的切换事件触发全状态受约束编队控制 — 中文详细总结

---

## 一、核心问题与研究背景

- **研究领域**：智能交通系统（ITS）中的多车辆（多智能体）编队控制。
- **核心问题**：  
  在车辆编队过程中，车辆必须保持安全间距与合理速度，然而当系统状态趋近约束边界时，传统基于障碍Lyapunov函数（BLF）的控制方法易出现：
  - 控制奇异性（控制律发散或趋近无限大）；
  - 过度控制输入与执行器饱和；
  - 数值不稳定与通信负担过重（周期通信或固定事件触发阈值导致高频更新）。
- **研究动机**：  
  现有方法无法在保证状态约束、安全性和通信效率之间取得平衡。本文旨在建立一种**同时满足状态约束与通信效率的鲁棒编队控制框架**。

---

## 二、方法论：核心思想与技术细节

### 2.1 总体框架

本文提出一种 **“平滑非线性变量映射 + RBF 神经网络近似 + 切换事件触发机制（SETM）”** 的综合自适应控制方法。

**总体逻辑：**
1. **平滑映射**将受约束状态空间（间距、速度）转为无约束空间；
2. 在该空间中设计**反步法（backstepping）自适应控制器**；
3. 利用**径向基函数神经网络（RBFNN）**在线估计未知非线性项；
4. 通过**切换事件触发机制（SETM）**动态调整通信频率；
5. **Lyapunov分析**用于稳定性和有界性证明。

---

### 2.2 关键技术细节

#### (1) 状态映射与约束处理
- 使用平滑**对数映射（logarithmic mapping）**处理速度与间距约束：
  \[
  s_{v,i} = \ln\left(\frac{v_i - v_{\min}}{v_{\max} - v_i}\right)
  \]
  类似公式用于间距 \(d_i\)。
- 映射的逆函数保证在物理空间中速度与距离保持于安全范围内。

#### (2) 动态模型与RBFNN近似
- 车辆的二阶非线性模型：
  \[
  \dot{p}_i = v_i, \quad \dot{v}_i = f_i(x_i) + g_i(x_i)u_i + d_i(t)
  \]
- 未知项 \(f_i(x_i)\) 由神经网络估计：
  \[
  f_i(x_i) = W_i^T \phi_i(x_i) + \varepsilon_i
  \]
  其中 \(\phi_i\) 为高斯基向量，\(\varepsilon_i\) 为有限逼近误差。

#### (3) 反步自适应控制设计
- 位置误差：\(z_{1i}\)，虚拟控制输入 \(\alpha_i = -K_{1i}z_{1i}\)
- 速度层误差：\(z_{2i} = s_{v,i} - \alpha_i\)
- 控制律：
  \[
  u_i = g_i^{-1}(x_i)[ -K_{2i}z_{2i} - \hat{W}_i^T\phi_i + \dot{\alpha}_i ]
  \]
- 参数更新律：
  \[
  \dot{\hat{W}}_i = \Gamma_i\phi_i z_{2i}^T - \sigma_{w,i}\hat{W}_i
  \]
  漏泄项 \(\sigma_{w,i}\) 防止参数发散。

#### (4) 切换事件触发机制（SETM）
- **触发条件**：
  \[
  \Psi_i = \|e_i(t)\|^2 - \sigma_i(t)\|z_{2i}(t)\|^2 \ge 0
  \]
- **切换阈值**：
  \[
  \sigma_i(t) =
  \begin{cases}
  \delta_1, & \|z_{2i}(t)\| \ge \epsilon\\
  \delta_2, & \|z_{2i}(t)\| < \epsilon
  \end{cases}
  \]
  其中 \(0 < \delta_1 < \delta_2 < 1\)，以在大误差时增加更新频率、在收敛区减少通信负载。

#### (5) 理论保证
- 若控制增益 \(k_2 > \frac{1}{2} + \sigma/2\)，系统满足：
  - 所有信号一致最终有界（Uniformly Ultimately Bounded, UUB）；
  - 状态约束永不违背；
  - Zeno行为被严格排除。

---

## 三、实验设计

- **软件环境**：MATLAB。
- **仿真场景**：四辆自动驾驶汽车（1 个领导者 + 3 个跟随者）的二维方形编队。
- **参数设定**：
  - 车辆质量介于 1700–1850 kg；
  - 仿真时长：50 s；
  - 采样周期：0.001 s；
  - 目标间距：12 m；
  - 领导车速变化：从 12 m/s 降至 6 m/s。
- **性能评估指标**：
  - 编队稳定性（位置、速度误差收敛速度）；
  - 状态约束保持性（是否超出设定边界）；
  - 通信效率（触发次数、通信负载降低率）。

- **对比参照**：未使用显式 baseline，但对比了周期性控制更新方案，作为通信负载对比 benchmark。

---

## 四、资源与算力

- 文中仅说明在 MATLAB 环境下进行数值仿真；**未提及 GPU 型号、算力配置或训练时间**。
- 因为只是控制算法验证，**无深度模型训练需求**。

---

## 五、实验数量与充分性

- 核心为单一仿真场景（四车方阵编队），时间跨度 50s。
- 实验包括：
  - 位置与速度跟踪；
  - 状态约束验证；
  - 事件触发间隔分析；
  - 通信负载统计表（四辆车总触发次数）。
- 虽未涉及参数敏感性或多场景实验，但：
  - 所有理论假设均通过数值验证；
  - 实验充分展示了约束保持、通信减少和无Zeno特性。
- 因此**实验在理论验证层面充分，但在多样化场景验证方面略显单一**。

---

## 六、主要结论与发现

- 提出的控制方法能在**状态约束条件下实现稳定编队控制**；
- **全部闭环信号保持一致最终有界（UUB）**；
- 自适应权值具有**漏泄抑制作用**，提高鲁棒性；
- 切换事件触发机制（SETM）：
  - 调节通信频率；
  - **通信负载减少超过 98%**；
  - 且所有触发间隔均为正，无Zeno行为；
- 仿真结果确认速度与间距均保持在安全范围内。

---

## 七、优点与亮点

- ✅ **创新性映射设计**：平滑非线性映射避免了BLF的奇异性问题。  
- ✅ **控制与通信一体化优化**：通过SETM实现动态通信调控，兼顾响应速度与效率。  
- ✅ **理论完整性强**：Lyapunov分析、误差界限、Zeno排除均具严格数学证明。  
- ✅ **高通信节省率**：实验显示通信更新次数减少约 98%，具有明显工程优势。

---

## 八、不足与局限

- ⚠ **场景单一**：仅验证了小规模（4车）方阵编队，未涵盖更复杂拓扑或多路环境。  
- ⚠ **缺乏实车或物理实验验证**：仅为 MATLAB 仿真，距离工程实现仍需验证。  
- ⚠ **缺少与主流算法（如BLF法或RL控制方法）的直接对比**，难以定量证明优越性。  
- ⚠ **参数选择经验性强**：SETM阈值的设计依赖人工设置，未探讨自适应优化机制。  
- ⚠ **未提供算力与实时性分析**，不利于评估系统的在线可扩展能力。

---

（完）
