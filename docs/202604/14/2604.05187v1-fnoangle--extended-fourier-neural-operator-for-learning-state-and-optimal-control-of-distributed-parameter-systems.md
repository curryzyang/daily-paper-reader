---
title: "FNO$^{\\angle θ}$: Extended Fourier neural operator for learning state and optimal control of distributed parameter systems"
title_zh: "FNO$^{\\angle θ}$：用于分布参数系统状态与最优控制学习的扩展Fourier神经算子"
authors: "Zhexian Li, Ketan Savla"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.05187v1"
tags: ["query:pde"]
score: 9.0
evidence: 用于学习偏微分方程状态和最优控制的扩展傅里叶神经算子
tldr: "该研究提出扩展傅里叶神经算子FNO^{∠θ}架构，用于学习由偏微分方程控制的系统状态和最优控制。通过将FNO的频率扩展到复数域，以捕捉Ehrenpreis-Palamodov原理中的复杂积分表示，从而在非线性Burgers方程实验中显著降低训练误差并提升非周期边界预测性能。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.05187v1/fig-001.webp\", \"caption\": \"Fig. 1: Comparison of relative mean squared errors (MSE) over all training data during the training phase of FNO and FNO∠θ for the Burgers’ equation (16). FNO∠θ achieves order of magnitude improvement over FNO.\", \"page\": 5, \"index\": 1, \"width\": 1052, \"height\": 314}]"
motivation: 为了有效学习偏微分方程控制系统中的状态和线性二次加性最优控制。
method: 将傅里叶变换的频率变量从实数域扩展到复数域，引入基于Ehrenpreis-Palamodov原理的复杂积分表征。
result: 在非线性Burgers方程上显著降低训练误差并提升非周期边界预测精度。
conclusion: 扩展的傅里叶神经算子能更稳定、准确地学习PDE系统状态与最优控制。
---

## 摘要
我们提出了一种扩展的Fourier神经算子（FNO）架构，用于学习由偏微分方程控制的系统的状态以及线性二次加性最优控制。借助Ehrenpreis-Palamodov基本原理，我们证明了具有常系数的线性偏微分方程的任意状态和最优控制都可以表示为复域中的积分。这种表示的被积函数包含与逆Fourier变换中的指数项相同的成分，后者用于表示FNO层中的卷积算子。受到这一观察的启发，我们通过将逆Fourier变换中的频率变量从实域扩展到复域来修改FNO层，以捕获基本原理中的积分表示。我们在非线性Burgers方程的状态与最优控制学习中展示了FNO的性能，相比传统FNO，训练误差数量级上得到显著改进，并在非周期性边界值预测中获得更高精度。

## Abstract
We propose an extended Fourier neural operator (FNO) architecture for learning state and linear quadratic additive optimal control of systems governed by partial differential equations. Using the Ehrenpreis-Palamodov fundamental principle, we show that any state and optimal control of linear PDEs with constant coefficients can be represented as an integral in the complex domain. The integrand of this representation involves the same exponential term as in the inverse Fourier transform, where the latter is used to represent the convolution operator in FNO layer. Motivated by this observation, we modify the FNO layer by extending the frequency variable in the inverse Fourier transform from the real to complex domain to capture the integral representation from the fundamental principle. We illustrate the performance of FNO in learning state and optimal control for the nonlinear Burgers' equation, showing order of magnitude improvements in training errors and more accurate predictions of non-periodic boundary values over FNO.

---

## 论文详细总结（自动生成）

## 一、研究背景与核心问题

- **研究动机**：分布参数系统（Distributed Parameter Systems, DPS），通常由偏微分方程（PDE）描述，广泛存在于流体动力学、气候建模及工程控制等领域。  
  传统最优控制方法往往面临维度灾难和高计算成本的问题。例如，离散化方法会破坏 PDE 模型的连续性并造成精度与算法复杂度的权衡。

- **研究问题**：能否构建一种端到端的神经算子网络，可以同时学习：
  - PDE 的**状态映射关系（state operator）**
  - PDE 的**最优控制算子（optimal control operator）**
  
  并在有限训练数据下对复杂边界条件（尤其是非周期边界）保持较高预测精度。

- **核心挑战**：标准 Fourier 神经算子（FNO）基于周期性卷积核构建，对非周期边界或复数域积分形式的 PDE 适应性不佳，因此其泛化与表达能力有限。

---

## 二、方法论与技术细节

### 1. 理论出发点

- 作者基于 **Ehrenpreis–Palamodov 基本原理**，指出：
  - 含常系数的线性 PDE 的任何解与其最优控制均可表示为 **在复数域上的积分形式**；
  - 该积分的核心项与 Fourier 逆变换的指数核 \( e^{i k\cdot\xi} \) 形式完全一致，但积分域在复平面上。

- 由此推断：若将 FNO 的 Fourier 频率变量从实数域扩展至复数域，可自然捕获更广泛的解空间结构，尤其适合非周期边界与复杂 PDE 控制问题。

### 2. 提出方法：FNO$^{∠θ}$（Extended Fourier Neural Operator）

- **关键思想**：
  - 扩展 FNO 层中的卷积算子，将实频率 \( k \) 替换为复变量：
    \[
    z_j(k, θ_l) = k_j e^{iθ_{l,j}},
    \]
    其中 θ 为新增的可学习相位参数。
  - 当 \( θ = 0 \) 时退化为传统 FNO。

- **算子定义**：
  \[
  (K^{θ}_l v_l)(ξ) = \frac{1}{(2π)^n} \int e^{i z(k,θ_l)·ξ} \hat{K}_l(k)\,\hat{v}_l(k)\,dk
  \]
  与标准 FNO 的卷积核一致，但积分频率位于复空间。

- **模型结构**：
  - 保留传统 FNO 的层次结构：**提升层 R → 多层线性–非线性算子 L_l → 投影层 Q**；
  - 在每一层中引入相位参数 θ_l，形成更具表达能力的复频卷积网络；
  - 当神经层数 \( L \) 和提升维度 \( n_v \) 较小时，FNO$^{∠θ}$ 已显著优于标准 FNO。

### 3. 理论支撑

- 作者证明线性 PDE 的状态与 LQR 型最优控制均能由复积分形式表示；
- 进一步说明该结构在 \( θ=0 \) 时保持 FNO 的**通用逼近性质（universal approximation property）**。

---

## 三、实验设计

### 1. 实验目标
- 验证 FNO$^{∠θ}$ 在学习非线性 PDE（Burgers 方程）状态与最优控制算子时的性能提升。

### 2. 数据与场景

- **方程类型**：一维非线性 Burgers 方程
  \[
  ∂_tϕ + ϕ ∂_xϕ = ν ∂_{xx}ϕ + u(x,t)
  \]
  其中 \( ν=0.05 \)，引入Shock波特征（非光滑性）。

- **空间/时间域**：\( Ω_x=[0,1] \)，\( Ω_t=[0,0.5] \)。

- **离散化**：
  - 空间步数 \( n_x=24 \)
  - 时间步数 \( n_t=30 \)

- **边界/初值条件**：
  - 初值：\( ϕ_0(x) = \sin(πx) \)
  - 边界 \( g(t), h(t) \)：由高斯随机场采样（均值为0，相关长度 ℓ=0.15，标准差1）。

- **生成真实数据**：
  - 通过有限差分法求解 PDE；
  - 训练数据：50 条状态轨迹；
  - 控制信号数据：使用伴随方法（adjoint method）计算最优控制。

### 3. 对比方法

- **Baseline**：原始 Fourier Neural Operator (FNO)
- **Proposed**：Extended Fourier Neural Operator (FNO$^{∠θ}$)

- 所有实验参数保持一致：  
  - 四层 Fourier 层 (L=4)  
  - 激活函数：GELU + Batch Normalization  
  - 提升维度 \( n_v = n_a \)，频率截断 \( M=4 \)。  

---

## 四、资源与计算配置

- 文中**未明确说明** GPU 类型、训练时长或具体算力资源。
- 仅指出实验在“有限数据集和轻量神经网络配置下”完成。
- 因此可推测研究重点在理论创新与架构验证层面，而非大规模并行训练。

---

## 五、实验数量与充分性

- **实验类型**：
  1. 状态映射算子学习（State Operator）
  2. 最优控制算子学习（Optimal Control Operator）
  
- **可视化分析**：
  - 均提供损失曲线（MSE 随训练迭代变化）；
  - 绘制预测误差热图，分析边界与内部误差分布。

- **结果数量**：
  - 每个任务 1 组主要实验；
  - 不同随机边界条件下共 50 个样本；
  - 未包含消融实验（如相位参数或频率数量敏感性分析）。

- **充分性评价**：
  - 实验数量有限但目标明确；
  - 初步验证了改进架构的有效性，但**泛化性和不同 PDE 类型的普适性**仍待后续验证。

---

## 六、主要结论与发现

1. **显著误差降低**：
   - 状态学习任务：FNO 平均相对 MSE = 0.039，FNO$^{∠θ}$ = 0.0089，提升约 4.4 倍。
   - 控制学习任务：FNO = 0.098，FNO$^{∠θ}$ = 0.042，提升约 2.3 倍。

2. **边界精度改进明显**：
   - 传统 FNO 因周期延拓效应导致边界误差集中；
   - FNO$^{∠θ}$ 能更准确地保持非周期边界连续性。

3. **表达能力增强**：
   - 扩展至复频域后，模型能表示更一般的 PDE 解结构；
   - 尤其在低维宽度条件下（\( n_v = n_a \)）仍具高精度性能。

---

## 七、方法优点与创新点

- **理论创新**：首次将 **Ehrenpreis–Palamodov 基本原理** 与神经算子设计相结合。
- **架构新颖**：引入复数域相位参数 θ，使 FNO 能学习非周期解和更一般的 PDE 控制算子。
- **性能提升显著**：训练误差数量级改善，并改善边界预测。
- **物理解释性强**：扩展后的复频结构具备明确的数学与物理对应性。

---

## 八、不足与局限

- **实验覆盖面不足**：仅在一维 Burgers 方程上验证，缺少多维复杂 PDE 测试。
- **超参数与算力未披露**：缺乏可重复性信息（训练时间、硬件配置等）。
- **未进行消融实验**：未展示 θ 参数或频率扩展对性能的逐步贡献。
- **应用推广限制**：当前仅在监督学习框架下评估，未来需探索无监督或强化学习型控制情境。
- **理论与实践脱节风险**：Ehrenpreis–Palamodov 表示为非构造性结果，参数学习在复杂系统中的可稳定性尚待验证。

---

**（完）**
