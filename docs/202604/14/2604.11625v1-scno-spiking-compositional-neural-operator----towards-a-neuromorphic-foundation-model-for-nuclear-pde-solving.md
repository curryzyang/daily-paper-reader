---
title: "SCNO: Spiking Compositional Neural Operator -- Towards a Neuromorphic Foundation Model for Nuclear PDE Solving"
title_zh: SCNO：脉冲组合神经算子——迈向用于核 PDE 求解的神经形态基础模型
authors: "Samrendra Roy, Souvik Chakraborty, Rizwan-uddin, Syed Bahauddin Alam"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11625v1"
tags: ["query:pde"]
score: 10.0
evidence: 用于求解耦合偏微分方程的脉冲组合神经算子
tldr: 本研究提出脉冲组合神经算子（SCNO），采用模块化神经组件与输入条件聚合器，通过组合已训练的基础微分模块解决未见耦合PDE，能在保持忘却零扩展的前提下显著降低误差和参数量。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.11625v1/fig-001.webp\", \"caption\": \"Figure 5: Coupled PDE predictions across all five systems and four methods. Each row is a coupled PDE; columns show different\", \"page\": 6, \"index\": 1, \"width\": 1044, \"height\": 1167}, {\"url\": \"assets/figures/arxiv/2604.11625v1/fig-002.webp\", \"caption\": \"Figure 1: SCNO architecture. Frozen spiking operator blocks (B∇ , BΔ, B𝑓 ), each containing three LIF layers with skip con-\", \"page\": 2, \"index\": 2, \"width\": 500, \"height\": 302}, {\"url\": \"assets/figures/arxiv/2604.11625v1/fig-003.webp\", \"caption\": \"Figure 8: Accuracy–efficiency Pareto analysis. SCNO+Corr (orange squares) achieves competitive accuracywith 5× fewer\", \"page\": 7, \"index\": 3, \"width\": 493, \"height\": 355}, {\"url\": \"assets/figures/arxiv/2604.11625v1/fig-004.webp\", \"caption\": \"Figure 6: Comparison across five coupled PDEs: (left) relative 𝐿2 error, (center) parameter counts on log scale, (right) spike\", \"page\": 7, \"index\": 4, \"width\": 1039, \"height\": 279}, {\"url\": \"assets/figures/arxiv/2604.11625v1/fig-005.webp\", \"caption\": \"Figure 3: Representative predictions from elementary spiking blocks on held-out test samples. Each row corresponds to one block (convection 𝑇𝑠=30, diffusion 𝑇𝑠=20, reaction 𝑇𝑠=20). Gray dashed: initial condition 𝑢0 (𝑥); blue: ground truth 𝑢 (𝑥,𝑇 ); red dashed: SCNO prediction. All blocks achieve sub-10% relative 𝐿2 error on unseen initial conditions.\", \"page\": 4, \"index\": 5, \"width\": 1043, \"height\": 596}, {\"url\": \"assets/figures/arxiv/2604.11625v1/fig-006.webp\", \"caption\": \"Figure 4: Nuclear case study: 1-group neutron diffusion 𝜕𝑡𝜙 = 𝐷𝜕𝑥𝑥𝜙 + (𝜈Σ𝑓 − Σ𝑎)𝜙 . Top: predictions vs. ground truth. Bottom: pointwise absolute error. SCNO (frozen, shown) achieves 9.4% relative 𝐿2 error; with correction, 4.6%.\", \"page\": 5, \"index\": 6, \"width\": 1044, \"height\": 445}, {\"url\": \"assets/figures/arxiv/2604.11625v1/fig-007.webp\", \"caption\": \"Figure 7: Forgetting-free modular expansion: blocks are\", \"page\": 5, \"index\": 7, \"width\": 491, \"height\": 303}]"
motivation: 传统神经算子模型能效低、适应性差，需针对每个新物理问题重新训练。
method: 提出一种结合脉冲与常规组件的模块化架构SCNO，用于组合训练在不同微分算子的神经模块来解决复杂PDE。
result: SCNO在八个PDE族中实现最低相对L2误差，参数量仅为单体基线的约五分之一。
conclusion: SCNO首次实现了具忘却保护的模块化神经算子，为高效类脑PDE求解奠定基础。
---

## 摘要
神经算子（Neural Operators）已经成为求解偏微分方程（PDE）的强大替代工具，但它们通常作为针对单个 PDE 的单体模型进行训练，需要高能耗的 GPU 硬件，并且在出现新物理机制时必须从零重新训练。我们提出了脉冲组合神经算子（Spiking Compositional Neural Operator, SCNO），这是一种结合脉冲与传统组件的模块化架构，可同时解决上述三种限制。SCNO 维持一个由小型脉冲神经算子模块组成的库，每个模块在单个基本微分算子（对流、扩散、反应）上进行训练，并通过一个轻量的、基于输入条件的聚合器（aggregator）将它们组合起来，以求解在模块训练期间未见过的耦合 PDE。一个小型校正网络在保持所有模块及聚合器冻结的情况下学习交叉耦合残差，从而在架构上实现无遗忘的模块化扩展。我们在八类 PDE 上评估了 SCNO，包括五类耦合系统以及具核能相关性的单群（1-group）中子扩散方程。带校正的 SCNO 在五个耦合 PDE 中的四个上实现了最低的相对 $L^2$ 误差，性能优于单体脉冲 DeepONet（平均提升达 62%，三次随机种子取均值）以及标准人工神经网络（ANN）DeepONet（最高提升达 65%），同时仅需 95K 个可训练参数，相比之下单体基准需 462K。 据我们所知，这是首个组合式脉冲神经算子，也是首次展示具有内建无遗忘扩展能力的模块化类脑 PDE 求解模型的概念验证。

## Abstract
Neural operators have emerged as powerful surrogates for partial differential equation (PDE) solvers, yet they are typically trained as monolithic models for individual PDEs, require energy-intensive GPU hardware, and must be retrained from scratch when new physics emerge. We introduce the Spiking Compositional Neural Operator (SCNO), a modular architecture combining spiking and conventional components that addresses all three limitations. SCNO maintains a library of small spiking neural operator blocks, each trained on a single elementary differential operator (convection, diffusion, reaction), and composes them through a lightweight input-conditioned aggregator to solve coupled PDEs not seen during block training. A small correction network learns cross-coupling residuals while keeping all blocks and the aggregator frozen, preserving zero-forgetting modular expansion by construction. We evaluate SCNO on eight PDE families including five coupled systems and a nuclear-relevant 1-group neutron diffusion equation. SCNO with correction achieves the lowest relative $L^2$ error on four of five coupled PDEs, outperforming both a monolithic spiking DeepONet (by up to 62%, mean over 3 seeds) and a standard ANN DeepONet (by up to 65%), while requiring only 95K trainable parameters versus 462K for the monolithic baseline. To our knowledge, this is the first compositional spiking neural operator and the first proof-of-concept for modular neuromorphic PDE solving with built-in forgetting-free expansion.

---

## 论文详细总结（自动生成）

# SCNO：脉冲组合神经算子——用于核 PDE 求解的类脑基础模型总结

---

## 一、研究动机与核心问题

- **研究背景：**  
  偏微分方程（PDE）在工程与物理系统中普遍存在，如流体力学、热传导、以及核反应堆的中子输运问题。近年来，**神经算子（Neural Operator）**成为数据驱动的 PDE 求解代理模型，如 DeepONet 和 FNO，它们能直接学习从初始条件函数到解空间的映射。

- **核心问题：**
  - **单体化问题**：现有神经算子通常为单体网络，每种 PDE 都需重新训练，无法跨物理任务复用知识。
  - **能耗与硬件问题**：传统神经算子依赖 GPU，大量能耗限制在边缘端和神经形态硬件上的部署。
  - **灾难性遗忘**：在多任务或顺序训练时，模型对先前 PDE 的性能急剧下降，不适合安全关键的系统（如核反应堆数字孪生）。

- **研究目标：**  
  本文提出一种**模块化、可组合、类脑计算友好**的神经算子框架——**Spiking Compositional Neural Operator（SCNO）**，在不重训练和不遗忘的前提下解决新 PDE 问题，尤其面向核工程中的 PDE 求解任务。

---

## 二、方法论：SCNO 架构与核心技术

### 1. 整体思想

SCNO 采用“**脉冲模块化组合学习**”策略：
- 将 PDE 的基本算子（如对流∇、扩散Δ、反应𝑓）分别用**独立的脉冲神经算子模块（Spiking Neural Operator Blocks）**学习；
- 然后通过一个**输入条件聚合器（Aggregator）**在更高层级组合多个块；
- 在面对强耦合 PDE 时，用一个**轻量校正网络（Correction Network）**学习残差项，而不修改已有模块，从而实现“**零遗忘（Zero Forgetting）**”。

### 2. 三阶段架构设计

1. **块库（Block Library）**：  
   - 每个块 \( B_k \) 都是一个针对单个基本算子的脉冲 DeepONet。  
   - 主干网络使用 Leaky Integrate-and-Fire（LIF） 神经元，含残差与可学习衰减。  
   - 例如，\( B_\nabla, B_\Delta, B_f \) 分别表示对流、扩散、反应模块。
   - 在训练后全部冻结，不再更新。

2. **输入条件聚合器（Aggregator）**：  
   - 聚合器根据输入条件特征 \( c=f_{ctx}(u_0) \) 动态组合多个块的输出：
     \[
     \hat{u}(y) = \sigma(g)\cdot \text{MLP}([B_{k_1},...,B_{k_K}; c]) + (1-\sigma(g))\cdot w^\top o(y)
     \]
   - 通过门控参数 \( g \) 控制非线性路径与线性路径的混合。

3. **校正网络（Correction Network）**：  
   - 非脉冲 MLP 结构（95K 参数），学习耦合残差：  
     \[
     G(u_0)(y) = \hat{u}(y) + \alpha C(u_0,y)
     \]
   - \(\alpha\) 为可学习权重（初始化 0.1），反映耦合强度。

### 3. 学习机制

- 所有 spiking 块及聚合器冻结后，新增物理过程只需训练新的块与校正网络，旧模块完全不变；
- 通过这种**架构性保证**，实现了完全的**无遗忘增量学习**；
- 推理阶段脉冲块运行在神经形态硬件上，校正网络在常规微控制器执行（异构部署）。

---

## 三、实验设计

### 1. 实验任务与场景
- 共八类 PDE 家族（含三类基础算子和五类耦合算子）：
  | 类型 | 方程示例 | 组合结构 |
  |-------|------------|-----------|
  | 对流 (Convection) | \( \partial_t u + c \partial_x u =0 \) | ∇ |
  | 扩散 (Diffusion) | \( \partial_t u = \nu \partial_{xx}u \) | Δ |
  | 反应 (Reaction) | \( \partial_t u = k u (1-u) \) | 𝑓 |
  | 对流–扩散 | \( ∇+Δ \) | |
  | 反应–扩散 | \( 𝑓+Δ \) | |
  | 中子扩散（核物理） | \( Δ+𝑓 \) | |
  | Burgers（非线性） | \( ∇+Δ \) | |
  | 对流–反应 | \( ∇+𝑓 \) | |

### 2. 数据与设定
- 空间网格：256 个点，时间步长 Δt=0.005，100 步；
- 每种 PDE 生成 1500 训练样本、400 测试样本；
- 初值：3–7 模随机傅里叶级数；
- 中子扩散场景的参数： \( D=1.0, Σ_a=0.1, νΣ_f=0.12 \)。

### 3. 对比基线
- **Monolithic SNN**：单一脉冲 DeepONet（462K 参数）；  
- **ANN DeepONet**：标准 ReLU DeepONet（396K 参数）；  
- 消融版本：**SCNO 无校正（frozen）** 与 **SCNO + Correction**。

---

## 四、算力与训练资源

- 论文未具体说明 GPU 型号、数量或训练时长；  
- 明确指出：
  - 模型训练仍在常规 GPU 上完成（使用 AdamW 与 surrogate gradient），未进行神经形态硬件上训练；
  - 推理阶段面向 **Intel Loihi 2** 芯片进行能耗估算（0.9 pJ/spike），推算得：
    - 单次推理约 9 nJ（SCNO） vs 4 nJ（单体 SNN）。

---

## 五、实验数量与充分性

- **实验总量：**
  - 八类 PDE × 四种模型 → 至少 32 组主要对比实验；
  - 还包含单块训练收敛曲线（3 类）、校正强度分析（α值）、能耗/参数对比（Fig.6–8）、零遗忘验证（Fig.7）；
  - 因此总体上实验广泛、系统且覆盖消融、可解释性与物理相关验证。
- **充分性与公平性：**
  - 所有模型在相同数据与超参数框架下训练；  
  - 提供多种随机种子（3次均值±标准差），结果波动小于0.5%，表现出良好稳定性。

---

## 六、主要结论与发现

1. **性能方面：**  
   - SCNO+Correction 在 5 类耦合 PDE 中的 4 类上取得最低误差：  
     - 反应–扩散：误差从 5.5% 降至 2.1%；  
     - 中子扩散：误差从 13.7% 降至 4.6%（降低 58%）；  
     - Burgers 与对流–反应也显著优于单体模型。  

2. **参数与能量效率：**
   - 训练参数仅 95K，相比单体模型减少近 5×；  
   - 推理能耗估计同样大幅降低，可分布式执行。

3. **可扩展性与无遗忘：**
   - 模块冻结机制保证旧任务性能恒定（误差完全一致）；  
   - 新算子模块加入后可自动扩展库，实现分阶段学习。

4. **物理解释性：**
   - 校正系数 \( \alpha \) 反映算子耦合强度（如反应–扩散 0.09，强耦合对流–反应 0.46）；  
   - 可作为自动化判别物理耦合程度的指标。

---

## 七、方法与实验的亮点

- **架构创新：**
  - 首次实现“**组合式脉冲神经算子**”，兼具神经形态能效与模块复用。
- **零遗忘特性：**
  - 模块冻结+聚合器独立训练，结构上保证无遗忘，而非依赖正则化。
- **可移植性与能源友好：**
  - 推理兼容 Loihi 2 等神经形态硬件；
  - 适合部署在低功耗核数字孪生或远程反应堆监控系统中。
- **理论–物理融合：**
  - 利用基本微分算子组成复杂 PDE，提供可解释的物理意义。
- **实验系统性强：**
  - 涵盖弱耦合与强耦合场景；
  - 提供定量与可视化分析（误差热图、性能–能耗帕累托曲线）。

---

## 八、局限与不足

- **适用范围限制：**
  - 所有实验仅限一维 PDE；高维场景未验证。
- **强耦合 PDE 精度仍有限：**
  - 对流–扩散误差高于单体模型（14.1% vs 10.7%）。
- **校正模块非脉冲化：**
  - 需额外传统计算资源，降低完全类脑化程度。
- **训练仍依赖 GPU：**
  - 虽推理能神经形态化，但训练使用代理梯度在 GPU 完成。
- **未与非脉冲组合模型（CompNO、LegONet）直接对比：**
  - 难以分离“脉冲化”与“组合化”的贡献。
- **能耗评估为估算值（基于文献能量模型），未在真实硬件测试。**

---

**（完）**
