---
title: Hierarchical Flow Decomposition for Turning Movement Prediction at Signalized Intersections
title_zh: 用于信号交叉口转向交通预测的分层流量分解方法
authors: "Md Atiqur Rahman Mallick, Kamrul Hasan, Pulock Das, Liang Hong, S M Shazzad Rassel"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09336v1"
tags: ["query:pde"]
score: 9.0
evidence: 预测交叉口的转向流量和走廊流量
tldr: 本文提出一种层次化深度学习框架HFD-TM，用于在信号控制路口预测转向流量。方法先预测走廊直行流量，再扩展到各转向流，并引入物理约束损失以保证流守恒。实测数据显示该模型比Transformer和GRU误差更低、训练更快，适用于实时交通控制。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.09336v1/fig-001.webp\", \"caption\": \"Figure 1: System-level overview of the proposed HFD-TM framework. Block A encodes corridorlevel through flows from historical inputs, Block B expands corridor predictions into turning movements using contextual information, and Block C refines predictions with residual correction and zero-movement masking.\", \"page\": 4, \"index\": 1, \"width\": 979, \"height\": 579}, {\"url\": \"assets/figures/arxiv/2604.09336v1/fig-002.webp\", \"caption\": \"Table 2: Ablation Study Results. ∆MAE is reported relative to the full HFD-TM model.\", \"page\": 8, \"index\": 2, \"width\": 746, \"height\": 311}, {\"url\": \"assets/figures/arxiv/2604.09336v1/fig-003.webp\", \"caption\": \"Table 1: Baseline comparison results. Errors are reported in vehicles per 15-minute interval.\", \"page\": 8, \"index\": 3, \"width\": 1125, \"height\": 222}, {\"url\": \"assets/figures/arxiv/2604.09336v1/fig-004.webp\", \"caption\": \"Figure 2: Comparison of mean absolute error (MAE) across baseline models and the proposed HFD-TM framework.\", \"page\": 9, \"index\": 4, \"width\": 932, \"height\": 574}]"
motivation: 转向流预测因方向流量波动大而困难，准确预测对自适应信号控制至关重要。
method: 提出层次化流分解模型，先预测主走廊流，再生成各转向流，并使用物理约束损失保持流一致性。
result: "在纳什维尔六个路口六个月LiDAR数据上，HFD-TM将MAE降低5.7%和27%，训练时间为DCRNN的1/12。"
conclusion: HFD-TM能显著提高转向流预测精度并具备实时应用潜力。
---

## 摘要
在信号交叉口准确预测转向交通流对于自适应信号控制至关重要，但由于方向流量的高波动性，这一任务依然困难。本文提出了一种分层深度学习框架 HFD-TM（Hierarchical Flow-Decomposition for Turning Movement Prediction），通过首先预测走廊直行流量，然后将这些预测扩展到各个转向流的方式来预测转向交通。这一设计受到交通结构实证规律的启发：走廊流量占总流量的 65.1%，其波动性低于转向流量，并能解释 35.5% 的转向流量方差。一个物理约束的损失函数通过强制流量守恒来保持结构一致性。我们在美国田纳西州纳什维尔的一个包含六个交叉口的走廊上，利用六个月的 15 分钟间隔 LiDAR（Light Detection and Ranging，光探测与测距）数据对该方法进行了评估。实验结果显示，HFD-TM 的平均绝对误差为每个时间间隔 2.49 辆车，相比 Transformer 模型降低了 5.7%，相比 GRU（Gated Recurrent Unit，门控循环单元）降低了 27.0%。消融实验结果表明，分层分解带来了最大的性能提升，而训练时间比 DCRNN（Diffusion Convolutional Recurrent Neural Network，扩散卷积循环神经网络）低 12.8 倍，展示了该方法在实时交通应用中的适用性。

## Abstract
Accurate prediction of intersection turning movements is essential for adaptive signal control but remains difficult due to the high volatility of directional flows. This study proposes HFD-TM (Hierarchical Flow-Decomposition for Turning Movement Prediction), a hierarchical deep learning framework that predicts turning movements by first forecasting corridor through-movements and then expanding these predictions to individual turning streams. This design is motivated by empirical traffic structure, where corridor flows account for 65.1% of total volume, exhibit lower volatility than turning movements, and explain 35.5% of turning-movement variance. A physics-informed loss function enforces flow conservation to maintain structural consistency. Evaluated on six months of 15-minute interval LiDAR (Light Detection and Ranging) data from a six-intersection corridor in Nashville, Tennessee, HFD-TM achieves a mean absolute error of 2.49 vehicles per interval, reducing MAE by 5.7% compared to a Transformer and by 27.0% compared to a GRU (Gated Recurrent Unit). Ablation results show that hierarchical decomposition provides the largest performance gain, while training time is 12.8 times lower than DCRNN (Diffusion Convolutional Recurrent Neural Network), demonstrating suitability for real-time traffic applications.

---

## 论文详细总结（自动生成）

## 一、研究背景与核心问题

- **研究动机**：  
  城市交通管理依赖于对交叉口 **转向交通流量（turning movement）** 的精确预测，这对于自适应信号控制与智能交通系统（ITS）至关重要。  
  然而，转向流量随时段、信号相位和上游扰动而剧烈波动，表现出强非线性和高方差特征，导致传统模型预测精度低、难以实时应用。

- **问题本质**：  
  现有方法（ARIMA、SVR、LSTM、Transformer等）多将各流量序列独立建模，忽视了**走廊-交叉口之间的层次依赖关系**与**流量守恒约束**。  
  因此，论文旨在提出一种能兼顾结构一致性与计算效率的模型，用以提升转向流量预测精度。

---

## 二、方法论：HFD-TM 分层流量分解框架

### 1. 核心思想

HFD-TM（Hierarchical Flow Decomposition for Turning Movement Prediction）是一种**两阶段分层预测框架**：

1. 首先预测相对平稳的 **走廊主线直行流量（corridor through-flows）**；
2. 然后利用这些预测结果作为条件，进一步扩展生成 **各个转向流量（turning movements）**。

这种设计符合交通结构规律——主走廊流量占总流量约 **65.1%**，其波动性较低并解释 **35.5%** 的转向流量方差。

### 2. 框架结构

模型由三大子模块组成：

- **Block A：Corridor Encoder**  
  以 GRU 为核心，对输入的时间序列提取走廊层面特征，输出走廊流量预测 ˆyc。

- **Block B：Turn-Ratio Predictor**  
  将走廊预测与“小时嵌入（hour-of-day embedding）”结合，通过线性映射与多层感知机（MLP）生成各转向流的比例修正。  
  数学形式：  
  $$ \hat{y}_{expand} = W_e \hat{y}_c + b_e, \quad \Delta \hat{y} = f_{MLP}([\hat{y}_c, e_h]) $$  
  $$ \hat{y}_{all} = \hat{y}_{expand} + \Delta \hat{y} $$

- **Block C：Residual Refinement + Zero Masking**  
  使用带权残差项（0.3×最近时刻）平滑预测，并通过两层轻量网络精炼结果。  
  最后应用零掩码 \( m \in \{0,1\}^N \) 去除结构上不可行的流向。

### 3. 损失设计（Physics-informed Loss）

采用三部分加权组合损失：
1. **预测误差损失 \( L_{mse} \)**：针对活跃流向；
2. **走廊加权损失 \( L_{corr} \)**：强化主流方向预测精度；
3. **流守恒约束 \( L_{cons} \)**：确保各交叉口进口与出口之和近似相等。  
   总损失：  
   $$ L = L_{mse} + \lambda_{corr} L_{corr} + \lambda_{cons} L_{cons} $$

### 4. 训练流程

使用 Adam 优化器（学习率 1e‑3，权衰减 5e‑4），配合学习率调度与早停策略。完整的训练—推理过程见 Algorithm 1（伪代码流程），包括批次训练、层次预测与损失反传。

---

## 三、实验设计

- **数据来源**：  
  使用美国田纳西州纳什维尔 Clarksville Pike 六个信号交叉口的 **LiDAR 实测交通数据**，共 6 个月、20,352 个 15 分钟间隔样本。  
  包含 72 个流向（12 个走廊直行 + 60 个转向）。

- **数据划分**：  
  时间顺序划分为训练 70%、验证 15%、测试 15%，防止信息泄露；特征均采用 min–max 归一化。

- **辅助输入**：  
  小时索引（0–23 h），以建模日常周期性交通波动。

- **评价指标**：  
  - **MAE（平均绝对误差）**
  - **RMSE（均方根误差）**
  - 模型训练时间（秒）

- **对比基线**：
  - GRU  
  - LSTM  
  - Transformer  
  - DCRNN（Diffusion Convolutional Recurrent Neural Network）

---

## 四、资源与算力情况

- 论文未明确说明使用的硬件资源（如 GPU 型号或数量）、内存或训练时长（小时数）。  
- 仅报告：HFD-TM 的训练耗时 **275.8 秒**，约为 DCRNN 的 **1/12.8 倍**，显示其计算效率显著。  
  因此可推断模型具备较低资源需求，适合实时部署。

---

## 五、实验数量与充分性

- **主实验**：基线对比 × 4 模型；
- **消融实验**：三组去除模块的对比（无层次结构、无走廊权重、无守恒约束）；  
  共 4 种配置，定量分析各组件贡献；
- **训练稳定性分析**：验证早停、收敛性能；
- **理论解释**：通过方差分解定量推导误差降低原理（Law of Total Variance）。

总体而言，实验设计系统、指标齐全，覆盖性能、计算效率及可解释性，结果客观且对比公平。

---

## 六、主要结论与发现

- **性能**：  
  - HFD-TM 在测试集 MAE = 2.4873、RMSE = 5.1886；
  - 相比 Transformer 降低 MAE 5.7%、相比 GRU 降低 27.0%。  
- **效率**：  
  - 训练时间比 DCRNN 减少 12.8 倍；
  - 收敛稳定于 60 epoch 内。  
- **解释性分析**：  
  通过建立“转向量条件于走廊流量”的层次关系，模型有效减少方差，符合交通统计结构。

---

## 七、方法与实验的优点

- **层次化设计**：符合现实交通规律，减少高波动特征学习难度；  
- **物理一致性**：加入流量守恒约束，提高预测结构合理性；  
- **轻量级与实时性强**：相比图神经网络，训练速度显著提升；  
- **全面验证**：包含基线比较与消融分析；  
- **理论解释充分**：结合统计方差原理说明误差下降来源。

---

## 八、不足与局限

- **数据范围有限**：仅在纳什维尔一条六交叉口走廊上验证，通用性待进一步探索；  
- **预测时域单步（15 分钟）**，未验证多步或长周期预测性能；  
- **地理多样性缺乏**：未含不同城市或道路形态；  
- **算力与硬件配置未披露**，限制了重复性评估；  
- **潜在偏差**：固定零掩码可能忽略临时交通改道、特殊事件等因素。

---

（完）
