---
title: "FNO$^{\\angle θ}$: Extended Fourier neural operator for learning state and optimal control of distributed parameter systems"
title_zh: "FNO$^{\\angle θ}$：用于学习分布参数系统状态与最优控制的扩展傅里叶神经算子"
authors: "Zhexian Li, Ketan Savla"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.05187v1"
tags: ["query:pde"]
score: 9.0
evidence: 扩展傅里叶神经算子用于学习偏微分方程的状态与最优控制
tldr: 本文提出扩展傅里叶神经算子（FNO^⟨θ⟩）架构，用于学习线性与非线性偏微分方程系统的状态和线性二次最优控制。该方法基于Ehrenpreis-Palamodov原理，将FNO层的傅里叶频率变量从实域扩展到复域，以更好地表征方程解和控制的积分表示。实验在非线性Burgers方程上验证了方法的显著性能提升，包括训练误差降低和非周期边界预测更精准。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.05187v1/fig-001.webp\", \"caption\": \"Fig. 1: Comparison of relative mean squared errors (MSE) over all training data during the training phase of FNO and FNO∠θ for the Burgers’ equation (16). FNO∠θ achieves order of magnitude improvement over FNO.\", \"page\": 5, \"index\": 1, \"width\": 1052, \"height\": 314}]"
motivation: 为解决分布参数系统中状态与控制的学习问题，提升现有FNO在复杂和非周期边界条件下的性能。
method: 通过将FNO层的傅里叶频率从实数域扩展到复数域，捕捉基于Ehrenpreis-Palamodov原理的积分表示。
result: 在非线性Burgers方程的实验中，训练误差降低一个数量级，预测非周期边界值更准确。
conclusion: 扩展后的FNO能够更精准地学习线性和非线性PDE系统的状态与控制，明显优于原始FNO。
---

## 摘要
我们提出了一种扩展的傅里叶神经算子（FNO）架构，用于学习由偏微分方程支配系统的状态以及线性二次加性最优控制。利用 Ehrenpreis-Palamodov 基本原理，我们证明了具有常系数的线性偏微分方程的任意状态和最优控制都可以表示为复数域中的一个积分。该积分表示的被积函数包含与逆傅里叶变换相同的指数项，而后者被用于表示 FNO 层中的卷积算子。受此启发，我们通过将逆傅里叶变换中的频率变量从实数域扩展到复数域来修改 FNO 层，以捕捉基本原理所导出的积分表示。我们通过非线性 Burgers 方程的状态与最优控制学习任务展示了该方法的性能，结果表明其训练误差降低了一个数量级，并在非周期边界值的预测上显著优于传统 FNO。

## Abstract
We propose an extended Fourier neural operator (FNO) architecture for learning state and linear quadratic additive optimal control of systems governed by partial differential equations. Using the Ehrenpreis-Palamodov fundamental principle, we show that any state and optimal control of linear PDEs with constant coefficients can be represented as an integral in the complex domain. The integrand of this representation involves the same exponential term as in the inverse Fourier transform, where the latter is used to represent the convolution operator in FNO layer. Motivated by this observation, we modify the FNO layer by extending the frequency variable in the inverse Fourier transform from the real to complex domain to capture the integral representation from the fundamental principle. We illustrate the performance of FNO in learning state and optimal control for the nonlinear Burgers' equation, showing order of magnitude improvements in training errors and more accurate predictions of non-periodic boundary values over FNO.

---

## 论文详细总结（自动生成）

# FNO$^{\angle θ}$：用于学习分布参数系统状态与最优控制的扩展傅里叶神经算子 — 论文总结

---

## 1. 核心问题与研究背景

- **研究主题**：本文研究如何利用神经算子（Neural Operators）学习由偏微分方程（PDE）支配的**分布参数系统**的状态与最优控制问题。  
- **研究动机**：  
  - 分布参数系统状态空间是无限维的，传统的最优控制方法（如LQR或PDE约束优化）计算代价极高。  
  - 神经算子提供了学习函数空间映射的可能性，可作为**无限维模型的快速代理**。  
  - 然而，现有傅里叶神经算子（Fourier Neural Operator, FNO）依赖傅里叶卷积形式，其**周期性假设限制了处理非周期边界条件和复杂域**的能力。  
- **核心问题**：如何设计一个可同时适应有界域和非周期边界条件的神经算子架构，用于学习PDE的状态及其最优控制算子。

---

## 2. 方法论：核心思想与关键技术

### 2.1 理论基础
- 作者利用 **Ehrenpreis-Palamodov 基本原理（EP原理）**，该原理指出：  
  - 对于**常系数线性PDE**，其解（包括状态与最优控制）可表示为**复数域上的积分形式**。  
  - 该积分包含与逆傅里叶变换相同的指数项 $e^{i k·ξ}$，但积分域由实空间扩展到复空间。

### 2.2 关键思路
- 传统 FNO 的卷积层通过实数傅里叶频率 $k$ 实现：  
  \[
  (K_l v_l)(ξ) = \frac{1}{(2π)^n} \int_{\mathbb{R}^n} e^{i k·ξ} \hat{K}_l(k)\hat{v}_l(k) \, dk
  \]
- 本文提出 **扩展傅里叶神经算子 FNO$^{⟨θ⟩}$**，将频率扩展为复数变量：
  \[
  z_j(k, θ_l) = k_j e^{iθ_{l,j}}, \quad θ_{l,j} \text{为可学习的相位参数}
  \]
  对应的卷积形式为：
  \[
  (K^{θ}_l v_l)(ξ) = \frac{1}{(2π)^n} \int_{\mathbb{R}^n} e^{i z(k,θ_l)·ξ} \hat{K}_l(k)\hat{v}_l(k) \, dk
  \]
- 当 $θ=0$ 时，模型退化为原始 FNO，因此保留其**通用逼近性质**。

### 2.3 算法架构
- **总体结构**：
  \[
  N(a) = Q \circ L_L \circ ... \circ L_1 \circ R(a)
  \]
  其中 $R$ 为输入升维操作、$Q$ 为降维输出映射、$L_l$ 为带有卷积核 $K_l^{θ}$ 的非线性算子层。
- **创新点**：
  - 在每一层中引入**复频率调制参数 θ**，增强算子层表示复杂边界与非周期解的能力；
  - 网络无需显著增加维度宽度 $n_v$ 即可提高表达能力。

---

## 3. 实验设计

- **目标 PDE**：非线性 Burgers 方程  
  \[
  ∂_t ϕ + ϕ ∂_x ϕ = ν ∂_{xx}ϕ + u
  \]
  其中粘度参数 ν = 0.05，边界与初始条件设置为光滑函数。  
- **任务类型**：
  1. 学习 **状态算子**（从边界条件到状态分布 $ϕ$）；
  2. 学习 **最优控制算子**（从边界条件到最优控制 $u^*$）。
- **数据集生成**：
  - 边界条件 $(g, h)$ 从均值为 0、长度尺度 0.15、标准差 1 的高斯随机场采样；
  - 采用有限差分法生成真实解（50 组状态轨迹）。
- **离散化**：
  - 空间 $n_x=24$，时间 $n_t=30$；
  - 频域离散频率 $k_m = -4 \ldots 4$（共 9 个频率点）。
- **对比方法**：
  - 原始 FNO；
  - 提出的 FNO$^{⟨θ⟩}$。
- **超参数设定**：
  - Fourier 层数 $L=4$；
  - 激活函数：GELU；
  - 批归一化（BatchNorm）；
  - 提升维度设为 $n_v = n_a$ 用于控制表达能力。

---

## 4. 资源与算力说明

- 文中**未具体说明**训练硬件、GPU类型、批量大小或训练时长；
- 仅提及训练基于标准离散化和小规模数据集；
- 因此可推测算力需求较低，适合单机训练实验。

---

## 5. 实验数量与充分性

- 论文共进行了两组主要对比实验：
  1. **状态算子学习**；
  2. **最优控制算子学习**。
- 各组实验含：
  - 损失曲线（相对MSE）；
  - 特定边界条件下的空间-时间误差可视化。  
- 未进行消融实验（例如去除 θ、不同 nv、不同 ν 等），也未测试多维 PDE。
- 实验证明概念可行，但**验证范围较窄**，缺乏综合 Benchmark 测试。

---

## 6. 主要结论与发现

- **理论结果**：基于 Ehrenpreis-Palamodov 原理，线性 PDE 的最优控制可表示为复域积分形式，这一结构可自然映射到扩展的 FNO 模型。  
- **实验结果**：
  - FNO$^{⟨θ⟩}$ 的训练误差比 FNO 低一个数量级；  
    - 状态学习：相对 MSE 从 0.039 → 0.0089；  
    - 控制学习：相对 MSE 从 0.098 → 0.042。  
  - 产生更精确的非周期边界预测，减小边界区域误差。
- **总体结论**：
  - 扩展复频率的 FNO 能更好地拟合有界域与复杂边界条件；
  - 提供一种可扩展的框架连接 PDE 理论表示与神经算子设计。

---

## 7. 方法优点与创新亮点

- **理论融合**：首次将 Ehrenpreis-Palamodov 基本原理与神经算子结合。  
- **结构创新**：通过**引入复频率相位参数 θ**，扩展FNO能力而参数量增长极小。  
- **边界适应性强**：显著提升对非周期边界的预测精度。  
- **通用性**：在 θ = 0 时退化为标准 FNO，兼容现有训练流程。  
- **效率优势**：无需大幅提升提升维度（宽度）即可获得显著误差改进。

---

## 8. 不足与局限

- **实验覆盖有限**：
  - 仅在一维 Burgers 方程上验证；
  - 缺乏在高维或复杂场景（如Navier-Stokes方程）上的结果。
- **评估不充分**：
  - 未报告计算资源、训练稳定性或泛化能力；
  - 无消融实验验证 θ 参数的独立贡献度。
- **理论适用范围**：
  - Ehrenpreis-Palamodov 表示严格成立于**线性常系数 PDE**；
  - 对非线性问题（此处 Burgers 方程）只是经验性有效。
- **数据规模较小**，可能难以衡量模型在真实复杂系统中的表现。

---

**（完）**
