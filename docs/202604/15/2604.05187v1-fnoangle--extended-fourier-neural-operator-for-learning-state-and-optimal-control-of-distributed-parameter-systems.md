---
title: "FNO$^{\\angle θ}$: Extended Fourier neural operator for learning state and optimal control of distributed parameter systems"
title_zh: "FNO$^{\\angle θ}$：用于分布参数系统状态与最优控制学习的扩展 Fourier 神经算子"
authors: "Zhexian Li, Ketan Savla"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.05187v1"
tags: ["query:pde"]
score: 10.0
evidence: 扩展傅里叶神经算子用于学习偏微分方程的状态与最优控制
tldr: 本文提出一种扩展的傅立叶神经算子（FNO^∠θ）模型，用于学习由偏微分方程控制的分布参数系统的状态和线性二次加性最优控制。该方法基于Ehrenpreis-Palamodov基本原理，将傅立叶变换的频率变量扩展到复数域，从而更准确地表示系统的复杂空间动态。实验表明，该模型在非线性Burgers方程上显著降低训练误差并提升非周期边界预测精度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.05187v1/fig-001.webp\", \"caption\": \"Fig. 1: Comparison of relative mean squared errors (MSE) over all training data during the training phase of FNO and FNO∠θ for the Burgers’ equation (16). FNO∠θ achieves order of magnitude improvement over FNO.\", \"page\": 5, \"index\": 1, \"width\": 1052, \"height\": 314}]"
motivation: 传统 FNO 在处理非周期边界和复杂 PDE 控制问题时效果有限。
method: 通过将傅立叶层的频率变量从实数域扩展到复数域，捕捉更完整的 PDE 表征。
result: 在非线性 Burgers 方程实验中，新模型的训练误差降低一个数量级并提高边界预测准确度。
conclusion: 扩展复域的傅立叶神经算子可以显著提高 PDE 系统状态和最优控制学习的精度。
---

## 摘要
我们提出了一种扩展的 Fourier 神经算子（FNO）架构，用于学习由偏微分方程支配的系统的状态和线性二次加性最优控制。通过采用 Ehrenpreis-Palamodov 基本原理，我们展示了具有常系数的线性偏微分方程的任意状态和最优控制可以表示为复域中的积分。这种表示的被积函数包含与逆 Fourier 变换中的指数项相同的形式，而逆 Fourier 变换在 FNO 层中用于表示卷积算子。受到这一发现的启发，我们通过将逆 Fourier 变换中的频率变量从实域扩展到复域，修改了 FNO 层，以捕捉该基本原理中的积分表示。我们通过非线性 Burgers 方程中的状态和最优控制学习实验，展示了 FNO 在训练误差数量级上的显著改进以及对非周期边界值预测的更高精度。

## Abstract
We propose an extended Fourier neural operator (FNO) architecture for learning state and linear quadratic additive optimal control of systems governed by partial differential equations. Using the Ehrenpreis-Palamodov fundamental principle, we show that any state and optimal control of linear PDEs with constant coefficients can be represented as an integral in the complex domain. The integrand of this representation involves the same exponential term as in the inverse Fourier transform, where the latter is used to represent the convolution operator in FNO layer. Motivated by this observation, we modify the FNO layer by extending the frequency variable in the inverse Fourier transform from the real to complex domain to capture the integral representation from the fundamental principle. We illustrate the performance of FNO in learning state and optimal control for the nonlinear Burgers' equation, showing order of magnitude improvements in training errors and more accurate predictions of non-periodic boundary values over FNO.

---

## 论文详细总结（自动生成）

# FNO∠θ：用于分布参数系统状态与最优控制学习的扩展 Fourier 神经算子 — 深度总结

## 一、核心问题与研究背景

- **研究动机**：  
  分布参数系统（Distributed Parameter Systems, DPS），如流体力学、气候建模等，常由偏微分方程（Partial Differential Equations, PDEs）描述，其状态与控制具有**无限维复杂性**。传统最优控制（如线性二次调节 LQR）在这类问题中计算成本高昂。

- **FNO 的局限**：  
  Fourier Neural Operator (FNO) 通过卷积在频域学习 PDE 的解算算子，但其傅立叶变换本质为**周期性表示**，难以准确逼近**非周期边界条件**或有界域问题。这导致：
  - 对复杂边界精度不足。
  - 需要提升网络宽度（lifted dimension）来弥补近似能力。

- **研究目标**：  
  本文希望通过理论扩展与网络结构创新，使神经算子能够表示更一般的 PDE 解与最优控制策略，尤其在**非周期、有界域**情况下提高精度与稳定性。

---

## 二、方法论：核心思想与关键技术

### 2.1 理论基础：Ehrenpreis–Palamodov 基本原理  
- 该原理指出：常系数线性 PDE 的任何解都可表示为**复域上指数函数的积分**。  
- 这一积分形式与逆傅立叶变换类似，但积分路径在**复数空间**而非实数域。

### 2.2 关键思想：复域扩展的 Fourier 神经算子  
- 原始 FNO 的卷积层通常表示为：
  \[
  (K_l v_l)(\xi) = \frac{1}{(2\pi)^n}\int_{\mathbb{R}^n} e^{i k\cdot\xi} \hat{K}_l(k)\hat{v}_l(k) \, dk
  \]
- 本文提出将频率变量 \(k\) 扩展为复数：
  \[
  z_j(k, \theta_l) = k_j e^{i\theta_{l,j}}
  \]
  其中 θ 是需要学习的新参数，用于调整复平面相位。

- 修改后的算子层：
  \[
  (K_{l}^{\angle\theta}v_l)(\xi)=\frac{1}{(2\pi)^n}\int_{\mathbb{R}^n} e^{i z(k,\theta_l)\cdot\xi} \hat{K}_l(k)\hat{v}_l(k) dk
  \]
  当 θ = 0 时，退化为传统 FNO。  
  这一扩展允许神经算子在复频空间学习 PDE 解与控制的更一般谱表示。

### 2.3 架构概述  
整体结构与传统 FNO 相同，由：
- 提升算子 **R**（lifting）；
- 若干 Fourier 层 **L₁,…,L_L**；
- 投影算子 **Q** 构成。
区别在于各层的卷积由**复域卷积**替代，使网络能表达更丰富的 PDE 解空间。

---

## 三、实验设计

- **任务类型**：学习 Burgers’ 方程的状态算子与最优控制算子。  
- **方程设置**：
  \[
  \frac{\partial \phi}{\partial t} + \phi \frac{\partial \phi}{\partial x} = \nu \frac{\partial^2 \phi}{\partial x^2} + u(x,t)
  \]
  其中粘性系数 ν = 0.05，时间范围 [0,0.5]，空间区间 [0,1]。

- **数据生成**：
  - 初始条件：φ₀(x) = sin(πx)。
  - 边界条件：从高斯随机场采样（长度尺度 ℓ=0.15，方差 σ=1）。
  - 数值解法：有限差分法生成真解。

- **任务划分**：
  1. **学习状态算子**：输入为边界条件，输出为 φ(x,t)。
  2. **学习最优控制算子**：输入为边界条件，输出为对应最优控制 u(x,t)。

- **对比模型**：  
  - **FNO∠θ（本文方法）**  
  - **标准 FNO 基线 [Li et al., 2020]**

- **性能指标**：相对均方误差 (Relative MSE)。

---

## 四、资源与算力

- 论文**未明确提供 GPU 型号、训练时间或计算资源**。  
- 提及的实现遵循 FNO 论文中的标准设置，频率截断参数 M=4，网络层数 L=4，激活函数为 GELU，并使用批归一化。  
- 由此推测实验运行在单 GPU 或中等算力环境中。

---

## 五、实验数量与充分性

- **数据规模**：50 组训练样本，每组包含不同边界条件下的解场。  
- **实验场景**：
  - 状态学习（Fig. 1a, Fig. 2）。
  - 最优控制学习（Fig. 1b, Fig. 3）。
- **分析内容**：
  - 对比训练误差收敛速度与最终 MSE。
  - 可视化误差分布（边界 vs 内部区域）。

> 无大规模参数敏感性实验或多 PDE 测试；消融实验仅体现在 θ 参数作用的比较。  
实验在受控小规模场景下**充分验证了原理性改进**，但尚未全面测试泛化能力。

---

## 六、主要结论与发现

- FNO∠θ 能在理论上刻画更一般的 PDE 解与控制形式（复域积分表示）。  
- 在 Burgers 方程实验中：
  - 状态学习相对 MSE：从 0.039 降至 0.0089；
  - 控制学习相对 MSE：从 0.098 降至 0.042；
  - 边界预测显著改善，非周期性误差大幅减少。
- 当网络宽度有限（nv ≈ na）时，复域扩展带来显著性能提升。

---

## 七、优点与创新点

- **理论创新**：  
  将 Ehrenpreis–Palamodov 数学原理与神经算子结构结合，首次提出**复频域神经层**设计。

- **结构改进**：  
  引入可学习的“相位参数 θ”，以复频形式拓展傅立叶层，促进非周期边界建模。

- **性能提升**：  
  训练误差数量级下降，边界预测更稳定，且无需增加网络宽度。

- **可解释性增强**：  
  复频扩展对应 PDE 理论中的复积分形式，使模型具有明确的数学物理动机。

---

## 八、不足与局限

- **实验覆盖有限**：仅在一维 Burgers 方程上验证，尚未测试更高维或多方程任务。  
- **算力及参数设置未披露**：难以评估效率、收敛性与可扩展性。  
- **无系统消融分析**：未独立评估 θ 参数维度、初值设置或不同复频路径对性能的影响。  
- **真实最优控制问题数据有限**：实验以数值仿真生成，不代表复杂工程控制情形。  
- **理论结果偏抽象**：Ehrenpreis–Palamodov 表示非构造性，复积分实现仍需近似假设。

---

**（完）**
