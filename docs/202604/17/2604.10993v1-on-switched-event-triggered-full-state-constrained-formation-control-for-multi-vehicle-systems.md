---
title: On Switched Event-triggered Full State-constrained Formation Control for Multi-vehicle Systems
title_zh: 多车辆系统的切换事件触发全状态约束编队控制研究
authors: "Zihan Li, Ziming Wang, Xin Wang"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.10993v1"
tags: ["query:profile-2"]
score: 9.0
evidence: 具有状态约束的智能交通系统车辆编队控制
tldr: 本文针对智能交通系统中多车辆编队控制的状态约束问题，提出了基于切换事件触发的自适应控制框架。通过平滑非线性映射避免约束边界奇异性，采用RBF神经网络在线逼近未知动力学，并结合反步法构建控制器，实现了稳定编队与通信效率提升。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.10993v1/fig-001.webp\", \"caption\": \"Fig. 2: Safety admissibility audit for longitudinal and lateral velocities of the AV formation.\", \"page\": 5, \"index\": 1, \"width\": 1030, \"height\": 423}, {\"url\": \"assets/figures/arxiv/2604.10993v1/fig-002.webp\", \"caption\": \"Fig. 1: Longitudinal and lateral tracking performance of the vehicle formation control and safe distances in control process.\", \"page\": 5, \"index\": 2, \"width\": 1035, \"height\": 312}, {\"url\": \"assets/figures/arxiv/2604.10993v1/fig-003.webp\", \"caption\": \"Fig. 3: The Longitudinal update time interval of SETM law for the AVs in square formation control process.\", \"page\": 6, \"index\": 3, \"width\": 493, \"height\": 378}]"
motivation: 现有车辆编队控制方法在处理速度和间距约束时易出现控制奇异性和高通信负担。
method: 采用状态空间平滑映射、RBF神经网络逼近及反步自适应控制结合切换事件触发机制。
result: 仿真结果表明，该方法实现了在状态约束下的稳定车队编队，并显著减少通信更新。
conclusion: 所提控制策略在保持系统稳定的同时能有效避免奇异性与过度控制，并降低通信负担。
---

## 摘要
车辆编队控制是智能交通系统（ITS）的重要组成部分。在实际应用中，控制器的设计需要满足包括车辆间距和车速在内的多个状态约束。当系统状态接近约束边界时，可能会出现控制奇异性和过大的控制努力，从而限制了现有方法的实际应用。为解决这一问题，本文针对具有不确定动力学的自主车辆（AV）的非线性车辆编队系统，提出了一种切换事件触发控制框架。首先引入一种平滑的非线性映射，将受约束的状态空间转换为无约束空间，从而避免了在约束边界附近出现的奇异性。接着，利用径向基函数神经网络（RBFNN）对未知的非线性动力学进行在线逼近，并在此基础上通过反步法构造自适应控制器。此外，设计了一种切换事件触发机制（SETM），在瞬态阶段增加控制更新频率，在稳态阶段降低通信负担。基于Lyapunov分析证明了闭环系统中所有信号保持一致有界，且不存在Zeno行为。仿真结果验证了所提出的方法能够在规定的状态约束下实现稳定的车队编队，同时显著减少通信更新次数。

## Abstract
Vehicular formation control is an important component of intelligent transportation systems (ITSs). In practical implementations, the controller design needs to satisfy multiple state constraints, including inter-vehicle spacing and vehicle speed. When system states approach the constraint boundaries, control singularity and excessive control effort may arise, which limits the practical applicability of existing methods. To address this problem, this paper investigates a class of nonlinear vehicular formation systems for autonomous vehicles (AVs) with uncertain dynamics and develops a switched event-triggered control framework. A smooth nonlinear mapping is first introduced to transform the constrained state space into an unconstrained one, thereby avoiding singularity near the constraint boundaries. A radial basis function neural network (RBFNN) is then employed to approximate the unknown nonlinear dynamics online, based on which an adaptive controller is constructed via the backstepping technique. In addition, a switched event-triggered mechanism (SETM) is designed to increase the control update frequency during the transient stage and reduce the communication burden during the steady-state stage. Lyapunov-based analysis proves that all signals in the closed-loop system remain uniformly bounded and that Zeno behavior is excluded. Simulation results verify that the proposed method achieves stable platoon formation under prescribed state constraints while significantly reducing communication updates.

---

## 论文详细总结（自动生成）

# 多车辆系统的切换事件触发全状态约束编队控制研究 — 中文总结

---

## 一、研究核心问题与背景

- **研究动机**：  
  智能交通系统（ITS）中的车辆编队控制（Vehicular Formation Control）旨在提高交通效率、节能与安全性。然而，当前控制方法在**速度与间距约束**条件下容易出现以下问题：  
  1. **控制奇异性**：靠近约束边界时，基于障碍Lyapunov函数（BLF）的惩罚项会急剧增大，导致控制量过大甚至出现奇异性。  
  2. **通信负担**：传统周期采样或固定阈值事件触发会产生冗余数据传输，不利于分布式车队的能效与实时性。  
- **核心问题**：如何在**不确定非线性动力学**和**严格速度/间距约束**条件下，设计一种既能保持**系统稳定与约束安全**、又能**降低通信更新频率**的控制策略。

---

## 二、方法论与技术细节

### 1. 整体框架

本文提出一种**切换事件触发（Switched Event-Triggered Mechanism, SETM）**的自适应控制框架，核心由三部分组成：

- **平滑非线性状态映射**：  
  将受限状态（如速度、间距）通过可逆平滑变换（logarithmic mapping）映射到无约束空间，避免传统BLF的奇异性：
  \[
  s_{v,i} = \ln\left( \frac{v_i - v_{\min}}{v_{\max} - v_i} \right)
  \]
  该映射在 \(v_{\min} < v_i < v_{\max}\) 范围内单调可逆，可保证约束不被破坏。

- **RBF 神经网络近似未知动力学**：  
  使用径向基函数神经网络（Radial Basis Function Neural Network, RBFNN）在线逼近未知的非线性项 \(f_i(x_i)\)，增强系统对模型不确定性和外部干扰的鲁棒性。

- **反步自适应控制（Backstepping Adaptive Control）**：  
  通过构造逐步Lyapunov函数，设计两层控制律：
  - 第一层：虚拟控制器用于位置层误差收敛；
  - 第二层：实际控制输入补偿速度层误差与神经网络逼近误差。

  同时引入**泄露项（leakage term）**以防止自适应权重发散。

### 2. 切换事件触发机制（SETM）

- 控制信号仅在触发时刻 \(t_{i,k}\) 更新，否则保持上一次采样值不变：
  \[
  u_i(t) = u_i(t_{i,k}), \quad t \in [t_{i,k}, t_{i,k+1})
  \]
- 触发条件：
  \[
  Ψ_i = \|e_i(t)\|^2 - \sigma_i(t)\|z_{2i}(t)\|^2 \ge 0
  \]
  其中 \(\sigma_i(t)\) 是**动态切换参数**：
  \[
  \sigma_i(t) = 
  \begin{cases}
  δ_1, & \|z_{2i}\| ≥ ϵ \\
  δ_2, & \|z_{2i}\| < ϵ
  \end{cases}
  \quad (0 < δ_1 < δ_2 < 1)
  \]
- 含义：
  - 当误差大 → 使用较小阈值 \(δ_1\) → 提高更新频率；
  - 当误差小 → 使用较大阈值 \(δ_2\) → 降低通信负担。

### 3. 稳定性证明

基于组合Lyapunov函数，推导得到系统满足**一致最终有界（UUB）稳定性**：
\[
\dot{V} \le -\lambda V + C
\]
并严格证明了：
- 自适应权重有界；
- 约束映射的逆变换保持物理变量合法；
- 最小触发间隔 \(T_{\min} > 0\)，从而**排除 Zeno 行为**。

---

## 三、实验设计

- **仿真环境**：  
  MATLAB 平台，车队包括 1 台领航车 + 3 台跟随车。
- **动力学模型**：  
  采用含未知扰动的二阶非线性纵向/横向动力学模型（考虑质量差异：1700–1850 kg）。  
  车速从 12 m/s 减速到 6 m/s，验证瞬态与稳态性能。
- **控制目标**：  
  方形编队（正方形阵列），保持安全间距 12 m，验证速度与位置双约束。
- **结果指标**：
  - 距离与速度是否保持在约束区间；
  - 控制触发次数；
  - 稳态误差；
  - 是否出现Zeno现象。
- **对比基准**：  
  与周期性采样控制进行通信负担对比（未显示具体对照算法，但通过节省通信百分比衡量性能改进）。

---

## 四、资源与算力

- **算力说明**：文中未提及具体硬件平台（如 GPU/CPU 型号）、运行时间或计算复杂度。  
  鉴于该工作为控制算法仿真（非深度学习训练），计算量主要集中于数值积分与 RBFNN 调整，可在常规 PC 上完成。

---

## 五、实验数量与充分性

- **实验类型**：
  - 方阵编队稳定性仿真；
  - 速度与间距安全性验证；
  - 动态（减速阶段）响应性能；
  - 事件触发时序与通信负担分析。
- **实验充分性**：  
  共展示 3 组主要结果（位置/速度/通信触发），辅以表格与图示分析。  
  虽未进行跨拓扑或噪声消融对比，但对目标任务的验证较完整。
- **公平性**：  
  以相同系统参数与扰动设置对比事件触发与周期触发控制，实验条件基本客观。

---

## 六、主要结论与发现

- 所提 SETM 控制框架能有效保证：
  - **所有闭环信号UUB有界**；
  - **速度与间距始终满足约束**；
  - **通信更新量降低超过 98%**；
  - **无 Zeno 现象**；
  - 能在车辆非线性动态和扰动下保持编队稳定。
- 理论证明与实验结果一致，表明该方法在智能车队控制中具有较高可行性。

---

## 七、优点与创新点

- **约束映射创新**：平滑可逆变换避免了BLF奇异性问题。  
- **自适应鲁棒性**：RBFNN 实现在线逼近未知动力学，提高抗干扰性。  
- **通信优化**：SETM 设计实现误差依赖切换阈值，自适应调整触发频率。  
- **理论完整性**：含Lyapunov稳定性证明、界限分析及无Zeno保证。  
- **工程实用性强**：适用于异构车辆编队、状态约束严格的交通应用。

---

## 八、不足与局限

- **实验单一**：仅在 MATLAB 仿真下测试，缺少硬件在环或真实车队实测。  
- **无多拓扑验证**：仅研究方阵拓扑，未分析链式、环式等更复杂结构。  
- **参数敏感性未深入**：未给出详细的阈值 \(δ_1, δ_2, ϵ\) 调节准则。  
- **通信信道模型理想化**：未考虑网络时延、丢包或攻击等通信不确定性。  
- **算力与实时性分析不足**：未报告算法在嵌入式平台上的计算负载表现。

---

**（完）**
