---
title: "FNO$^{\\angle θ}$: Extended Fourier neural operator for learning state and optimal control of distributed parameter systems"
title_zh: "FNO$^{\\angle θ}$：用于学习分布参数系统状态与最优控制的扩展傅里叶神经算子"
authors: "Zhexian Li, Ketan Savla"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.05187v1"
tags: ["query:pde"]
score: 9.0
evidence: 用于学习由偏微分方程支配的系统状态和最优控制的扩展FNO
tldr: 本文提出一种扩展的傅里叶神经算子（FNO^⟨θ⟩），用于学习由偏微分方程描述的系统状态与线性二次最优控制。基于Ehrenpreis-Palamodov原理，作者将频域从实数扩展到复数域，以更准确表达状态与控制的积分形式实现。新架构在Burgers方程的学习中显著降低训练误差，并提高非周期边界预测精度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.05187v1/fig-001.webp\", \"caption\": \"Fig. 1: Comparison of relative mean squared errors (MSE) over all training data during the training phase of FNO and FNO∠θ for the Burgers’ equation (16). FNO∠θ achieves order of magnitude improvement over FNO.\", \"page\": 5, \"index\": 1, \"width\": 1052, \"height\": 314}]"
motivation: 提升传统FNO在非周期边界和复杂控制任务中的学习精度。
method: 通过将FNO层的逆傅里叶变换频率变量扩展至复数域，捕捉PDE解的积分表示。
result: 在非线性Burgers方程实验中，新方法训练误差降低一个数量级，预测更为准确。
conclusion: 扩展到复频域的FNO能更精确地学习分布参数系统的状态与最优控制。
---

## 摘要
我们提出了一种扩展的傅里叶神经算子（FNO）架构，用于学习由偏微分方程控制的系统状态和线性二次附加最优控制。借助 Ehrenpreis-Palamodov 基本原理，我们证明了具有常系数的线性偏微分方程的任何状态与最优控制都可以在复数域中表示为积分形式。该表示的被积函数包含与逆傅里叶变换中相同的指数项，而逆傅里叶变换被用于在 FNO 层中表示卷积算子。受这一发现的启发，我们通过将逆傅里叶变换中的频率变量从实域扩展到复域来修改 FNO 层，以捕捉源自基本原理的积分表示。我们在非线性 Burgers 方程上展示了 FNO 在学习状态与最优控制方面的性能，结果表明训练误差降低了一个数量级，并在非周期边界值的预测上取得了更高的准确度，相较于原始 FNO。

## Abstract
We propose an extended Fourier neural operator (FNO) architecture for learning state and linear quadratic additive optimal control of systems governed by partial differential equations. Using the Ehrenpreis-Palamodov fundamental principle, we show that any state and optimal control of linear PDEs with constant coefficients can be represented as an integral in the complex domain. The integrand of this representation involves the same exponential term as in the inverse Fourier transform, where the latter is used to represent the convolution operator in FNO layer. Motivated by this observation, we modify the FNO layer by extending the frequency variable in the inverse Fourier transform from the real to complex domain to capture the integral representation from the fundamental principle. We illustrate the performance of FNO in learning state and optimal control for the nonlinear Burgers' equation, showing order of magnitude improvements in training errors and more accurate predictions of non-periodic boundary values over FNO.

---

## 论文详细总结（自动生成）

# 论文总结：FNO$^{⟨θ⟩}$ —— 用于学习分布参数系统状态与最优控制的扩展傅里叶神经算子

---

## 一、核心问题与研究背景

- **研究动机**：分布参数系统（Distributed Parameter Systems）由偏微分方程（PDEs）描述，广泛存在于流体动力学、气候模拟与工程控制等领域。这类系统具有无限维状态空间，使得状态预测及最优控制计算具有极高的复杂度。  
- **现有挑战**：
  - 传统的最优控制方法需要对空间域进行离散化，导致精度与计算效率之间的权衡。
  - 已有神经算子（Neural Operator），尤其是**傅里叶神经算子（Fourier Neural Operator, FNO）**，在学习 PDE 解方面表现突出，但其**周期性特征**限制了对**非周期边界条件**和复杂控制的刻画。
- **论文目标**：基于 **Ehrenpreis–Palamodov 基本原理**，将傅里叶神经算子的频域扩展到复数域，从而提升其对一般有界域和非周期边界问题的表达能力，实现对 PDE 状态与最优控制的统一学习。

---

## 二、方法论与技术细节

### 1. 理论基础

- **Ehrenpreis–Palamodov 原理**：任何常系数线性 PDE 的解可在复数域上表示为指数函数与多项式的积分形式。该积分包含与逆傅里叶变换中相同的指数项。
- **启发式核心**：由于 FNO 层的卷积算子正是由逆傅里叶变换定义，因此可以通过**将频率变量从实数域扩展到复数频域**，捕获这一更一般的积分表示。

### 2. 提出的方法：FNO$^{⟨θ⟩}$

- **架构概述**：在原 FNO 中，线性算子层的卷积核表示为：
  \[
  (K_l v_l)(ξ) = \frac{1}{(2π)^n} \int_{ℝ^n} e^{i k·ξ} \hat{K_l}(k)\hat{v_l}(k) \, dk
  \]

- **扩展后的形式**：在 FNO$^{⟨θ⟩}$ 中引入复频变量 \( z(k, θ) = k e^{iθ} \)，即：
  \[
  (K^{θ}_l v_l)(ξ) = \frac{1}{(2π)^n} \int_{ℝ^n} e^{i z(k,θ_l)·ξ} \hat{K_l}(k)\hat{v_l}(k) \, dk
  \]
  - 当 \( θ = 0 \)，此结构退化为标准 FNO。
  - 每层的参数包括：
    - 权重矩阵 \( W_l \)
    - 频域参数 \( \hat{K_l}(k) \)
    - 新增相位参数 \( θ_l \)
  - 通过学习复域相位参数，网络可以表示更复杂的带边界或非周期解。

### 3. 理论贡献

- 作者进一步证明：线性二次型最优控制问题的最优状态与控制也可表示为同类的复域积分形式。这为 FNO$^{⟨θ⟩}$ 同时学习**状态与控制算子**提供了理论依据。

---

## 三、实验设计

### 1. 实验场景

- **测试方程**：非线性 **Burgers 方程**（常用于神经算子性能验证）。
  - 空间域 \( Ω_x = [0,1] \)，时间域 \( Ω_t = [0, 0.5] \)
  - 设黏性系数 ν = 0.05 （生成非平滑冲击波行为）
  - 初始条件 \( ϕ_0(x) = \sin(πx) \)
  - 边界条件 \( g(t), h(t) \) 由高斯随机场生成。

### 2. 数据与对比方法

- **训练数据生成**：
  - 网格：空间点 24，时间点 30（即 \(24×30\) 网格）
  - 50 组随机边界条件，对应数值求解得到的真实状态与控制。
- **对比方法**：
  - 标准 **FNO**
  - 扩展版 **FNO$^{⟨θ⟩}$**
- 激活函数：**GELU**
- 归一化：批归一化（Batch Normalization）
- 层数：L = 4（推荐自 FNO 文献）
- 卷积频率采样数：M = 4（对应频率 km = -4 ~ 4）
- 提升维度：选择 nv = na，以验证扩展结构在低宽度条件下的表现。

### 3. 评价指标

- **主要指标**：相对均方误差（Relative MSE）  
- 同时比较空间—时间域内的绝对误差分布，以分析边界预测性能。

---

## 四、资源与算力信息

- 论文并**未明确说明硬件配置或算力资源**：
  - 未提及 GPU 型号、训练时间或批次大小。
  - 仅提供网络架构与参数设置。  
  → 因此无法对算力或训练效率做定量分析。

---

## 五、实验数量与充分性

- 实验共分为两大类：
  1. **状态算子学习**（50 条轨迹）
  2. **最优控制算子学习**（50 条轨迹）
- 每类任务包含：
  - FNO 与 FNO$^{⟨θ⟩}$ 的训练比较；
  - 图像结果分别展示了 MSE 曲线与空间误差分布。
- **未包含**：
  - 不同维度扩展实验；
  - 消融实验（如不同 θ 参数配置影响）。
- 实验数量有限，但对比公平、设置一致，能够说明关键性能提升。

---

## 六、主要结论与发现

1. **理论层面**：  
   - 状态与最优控制均可表为复域积分，与逆傅里叶变换结构一致，为傅里叶算子扩展提供数学依据。

2. **模型层面**：  
   - FNO$^{⟨θ⟩}$ 在卷积层中引入可学习复相位参数，实质上将实频域拓展至复频空间。

3. **实验结果**：
   - **学习状态算子**：
     - 训练后 MSE：FNO = 0.039，FNO$^{⟨θ⟩}$ = 0.0089 → 提升约 4.4 倍。
     - FNO 在边界处误差集中，表现出周期性失配；新方法显著改进边界预测。
   - **学习最优控制**：
     - 训练后 MSE：FNO = 0.098，FNO$^{⟨θ⟩}$ = 0.042 → 提升约 2.3 倍。
     - FNO$^{⟨θ⟩}$ 减少了空间域错误，但仍存在局部边界异常。
   - **总体结论**：引入复相位参数能显著提高表达能力和非周期边界一致性。  

---

## 七、主要优点

- **理论创新**：首次从 Ehrenpreis–Palamodov 原理中导出神经算子结构的扩展理论，具备数学上的充分性。
- **结构简洁**：仅需在 FNO 层中添加相位参数 θ，无需增加网络宽度即可明显提升性能。
- **适应性强**：能处理非周期或有界域问题，弥补传统 FNO 的核心缺陷。
- **实验效果显著**：在同样算力与网络规模下，训练误差降低一个数量级。

---

## 八、不足与局限

- **实验覆盖不足**：
  - 仅评估单一的 1D Burgers 方程，未验证更高维或多物理场问题。
  - 未测试不同 θ 参数分布或复域采样策略的稳定性。
- **算力未披露**：
  - 论文未说明硬件配置、训练时长，无法评价效率代价。
- **理论部分非构造性**：
  - Ehrenpreis–Palamodov 原理提供存在性，但积分形式无显式构造，可能限制实际解释性。
- **边界误差残留**：
  - 在最优控制实验中仍有边界局部误差，表明方法在复杂边界控制下仍有提升空间。
- **缺乏无监督或真实数据验证**：
  - 仅依赖数值合成数据，未探索实际物理数据或无标注场景。

---

**（完）**
